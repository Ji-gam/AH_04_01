# Task ID: T-MED-12 (기관명 필터 — 조사 붙은 케이스 구제)

### 배경

T-MED-11에서 추가한 `_INSTITUTION_SUFFIX_PATTERN`이 문자열 "끝"에만 앵커링(`$`)돼 있어서, 실제
배포된 CLOVA 키로 재현한 결과 "특정 기관의 연구 결과에 따른 것으로 본 약국의 의견과는 다를 수
있습니다"라는 처방전 문구에서 "약국의"(약국+조사 "의")가 필터를 통과함. 한국어 조사는 띄어쓰기
없이 명사에 바로 붙기 때문에, 끝 앵커 방식으로는 "필독약국"처럼 조사 없는 케이스만 잡히고
"약국의"/"약국은" 같은 케이스는 새어나감. 그 결과 T-MED-11 이전 버그로 이미 DB에 남아있던 쓰레기
레코드("SAMPLE*약국", `AUTO_E89FC2A23C`)와 계속 유사도 매칭됨.

### 참조

- 관련 코드: `app/services/medication_service.py` (`_INSTITUTION_SUFFIX_PATTERN`)
- 선행 작업: T-MED-11
- 관련 이슈: #120

### 범위

- **포함**: `_INSTITUTION_SUFFIX_PATTERN`을 끝 앵커(`$`) 대신 "텍스트 어디에든 포함되면 제외"로
  완화. 이전 버그로 이미 생성된 쓰레기 레코드(`AUTO_E89FC2A23C`, "SAMPLE*약국") DB 삭제(스케줄
  미연결 확인됨, 코드 변경과 별개로 운영 DB에서 직접 처리). 추가로, 실제 CLOVA 키로 프론트엔드에서
  재검증하던 중 같은 계열의 새 실패 케이스 발견: "아스피린에 과민증이 있는 경우..." 같은 복약안내
  문구에서 성분명+조사("아스피린에")가 완결된 약품명이 아닌데도 편집거리상 실제 등록된 다른 약
  ("아스피린정")과 1글자 차이라 임계값을 넘겨 오매칭됨 — 조사(은/는/이/가/을/를/도/만/와/과/의/에/로/나
  및 에서/부터/까지/으로/처럼/이나)로 끝나는 토큰을 퍼지 매칭에서 제외하는 `_TRAILING_PARTICLE_PATTERN`
  추가.
- **제외**: 조사 목록을 완전히 망라하는 형태소 분석기 도입(과설계 — 흔한 조사 블록리스트로 충분).

### 완료 정의 (Definition of Done)

- [x] "본 약국의 의견과는..."처럼 조사가 붙은 기관명도 퍼지 매칭에서 제외된다
- [x] "아스피린에"처럼 조사가 붙은 성분명 언급이 무관한 다른 실제 약과 퍼지 매칭되지 않는다
- [x] 기존 T-MED-11/T-MED-9 케이스(불릿 있음/없음, 오탈자 구제)는 회귀 없이 그대로 통과한다
- [x] (공통) 테스트를 TDD로 작성했고 `uv run pytest`가 통과한다(217 passed)
- [x] (공통) `ruff check`/`ruff format --check`/`mypy` 통과
- [x] 실제 배포된 CLOVA 키 + 실제 처방전 사진으로 프론트엔드에서 직접 재검증(임시 반영 후 확인)

### 완료 보고

- 완료 정의 체크리스트: 전부 통과
- 가정: 실제 한국어 약품명에 "약국/병원/의원/한의원"이 부분문자열로 포함되거나, 흔한 조사로 끝나는
  경우는 없다고 가정(과거 마스터 DB 데이터 기준 확인됨)
- 공유 계약 변경 필요 사항: 없음
- 브랜치명: `feature/T-MED-12-institution-filter-particle-fix`
