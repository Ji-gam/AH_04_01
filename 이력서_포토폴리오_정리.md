# ReMedi: 건강관리 플랫폼 — 이력서 & 포토폴리오 내용

---

## 📋 이력서 버전 (간결형)

### 프로젝트명
**ReMedi: 개인화 건강관리 플랫폼**

### 프로젝트 기간
2026년 4월 ~ 2026년 7월 (약 4개월)

### 팀 규모
- 총 8명 (프론트엔드 2명, 백엔드 3명, AI/LLM 2명, PM 1명)
- **담당 역할**: 기획 담당자 (벤치마킹 / 요구사항정의 / 화면설계) + 풀스택 개발 (프론트엔드 & 백엔드)

### 프로젝트 소개
**어르신 중심 만성질환 관리 플랫폼**
- 복약 알림, 식단·운동·수면 기록, AI 기반 주간 리포트, 질병별 맞춤 생활습관 추천
- 사용자 특성: 50~70대 어르신 (터치 민감도, 간단한 UI 중심)
- 목표: 복약순응도 향상, 생활 습관 개선, 의료진과의 정보 공유

### 📊 주요 성과

| 항목 | 수치 |
|---|---|
| **개발 PR** | 15개 (회귀 0개) |
| **마이그레이션** | 15개 (무중단 배포) |
| **신규 기능** | 알림 6개 + 개인화 9개 |
| **테스트 커버리지** | 20+ 신규 테스트 |
| **코드 변경량** | 4,000+ 라인 추가/수정 |
| **품질 체크 통과** | ruff / mypy / tsc / eslint 100% |

### 기술 스택

**Backend**
- Python 3.11, FastAPI, SQLAlchemy, Alembic
- MySQL 8.0, Redis (캐싱)
- Pydantic (데이터 검증)
- AI Worker Gateway (Claude API, 구조화 출력)

**Frontend**
- TypeScript, React 18, Vite
- TanStack Query (서버 상태), Zustand (클라이언트 상태)
- Tailwind CSS, Headless UI

**DevOps & 품질**
- Ruff (Python formatter/linter)
- MyPy (타입 체크)
- pytest (단위 테스트)
- ESLint, Prettier (JavaScript)

### 🎯 담당 영역

#### 기획 단계 (2주)
1. **벤치마킹** (닥터다이어리, 나의건강정보기록 분석)
2. **데이터 API 서치** (식약처 식품DB, 지도 API 등)
3. **요구사항정의서 작성** (필수/선택 기능 분류, TASK 정의)
4. **Figma 화면설계** (14개 화면, 사용자 플로우)

#### 개발 단계 (12주)
- **알림 시스템**: 6개 PR (복약 액션 / 스누즈 / 알림함 / 딥링크)
- **식단 개인화**: Mifflin-St Jeor 공식 기반 칼로리 계산 + AI 이유 생성
- **습관 추천**: 질병별 우선순위 로직
- **목표 관리**: F-GOAL 1/2/3 전체 구현 (CRUD + AI 가이드)
- **수면 기록**: 수면 시간/질 기록 + 홈 위젯
- **다이어리**: 식단/운동/일기/주간 AI 리포트
- **UI 통일화**: 14개 화면 제목 아이콘 + 버튼 모서리 표준화

### 📌 핵심 기술 & 의사결정

1. **AI 캐싱 전략**: (profile_id, log_date) 유니크로 일일 1회 AI 호출 → 비용 1/5 절감
2. **폴백 설계**: AI 실패 시 규칙 기반 템플릿 (100% 가용성)
3. **스마트 알림 묶음**: 같은 시각 알림을 1개로 통합 (배터리 소비 ↓)
4. **PII 분리**: HealthProfile 별도 테이블 (규제 준수)
5. **결정론적 로테이션**: 같은 사용자는 매일 같은 습관, 예측 가능성 ↑

### 🔗 포트폴리오 링크
- GitHub: [최종 PR #328 + 전체 커밋](링크)
- 시연 영상: (발표 때 라이브 또는 녹화본)

---

---

## 📖 포토폴리오 버전 (상세형)

# ReMedi: 개인화 건강관리 플랫폼

## 1. 프로젝트 개요

### 배경 & 동기
**어르신들의 복약순응도 문제**
- 통계: 65세 이상 어르신 중 만성질환 약을 빠뜨리는 비율 40% 이상
- 기존 앱들의 문제: 너무 많은 기능 (닥터다이어리) 또는 복잡한 인증 (나의건강정보기록)
- **우리의 솔루션**: 간단하지만 강력한 복약알림 + 건강데이터 통합 + 개인화된 생활습관 추천

### 타겟 사용자
- 연령: 50~70대 어르신
- 특성: 스마트폰 사용 가능하지만 복잡한 UI에 약함, 터치 민감도 낮음
- 주요 니즈: 약 먹는 시간 알림, 혈당/혈압 기록, 의료진과 정보 공유

### 프로젝트 규모
- **기간**: 2026년 4월 ~ 2026년 7월 (16주)
- **팀**: 8명 (기획 1명 + 개발 6명 + PM 1명)
- **성격**: 학습 프로젝트 (K-Digital 수료생 포트폴리오)

---

## 2. 기획 단계 (2주 담당)

### 2.1 벤치마킹 & 분석

#### 닥터다이어리 분석
```
장점:
  ✓ 혈당기·혈압계와 블루투스/핀번호로 자동 연동
  ✓ 혈당 15회 이상 입력 시 당화혈색소(HbA1c) 예측 제공
  ✓ 약물 상호작용 경고 시스템

한계점 (어르신 입장에서):
  ✗ 기능이 너무 많음 (포인트, 쇼핑몰, SNS 등)
  ✗ UI가 복잡 → 실제 어르신들은 "약 알림"만 필요
  ✗ 초기 설정이 복잡함 (온보딩 단계 많음)

우리의 차별화:
  → 핵심만 뽑아 간단하게 (90% 이상 사용 기능 5개 이내)
  → 15단계 설정 → 3단계로 단순화
```

#### 나의건강정보기록 분석
```
장점:
  ✓ 정부 공식 앱 (신뢰도 높음)
  ✓ 진료기록, 처방전, 검사 결과를 한 곳에서 조회

한계점:
  ✗ 간편로그인(카카오/네이버)이 어르신들에게 어려움
  ✗ 정보 조회 중심 → 능동적 관리(기록) 기능 부족
  ✗ 의료진과 직접 공유 기능이 없음

우리의 해결:
  → 회원가입을 2단계로 단순화 (이메일 + 비번)
  → 기록 → 분석 → 피드백 전체 사이클 제공
```

### 2.2 데이터 API 서치 & 검증

| API | 용도 | 선택 여부 | 사유 |
|---|---|---|---|
| **식약처 식품영양성분DB** | 음식 영양정보 검색 | ✅ 채택 | 정부공식, 무료, 15만 식품 포함 |
| **카카오 지도 API** | 응급 병원/약국 검색 | ✅ 채택 | 높은 정확도, 어르신 친화적 |
| **기상청 초단기 예보** | 날씨 알림 | ❌ 미채택 | 중요도 낮음 → 차후 확장 |
| **연속혈당측정기(CGM) API** | 혈당 자동 동기화 | ❌ 미채택 | 의료기기 승인 필요, 초기 범위 밖 |
| **FHIR 표준** | 의료 기록 표준화 | ✅ 검토 | 추후 병원 연동 시 필수 |

**결과**: 식약처 DB + 카카오 지도 API로 MVP 구축, 차후 확장 로드맵 수립

### 2.3 요구사항정의서 작성

#### 요구사항 분류 (MoSCoW 방식)

**Must Have (필수)**
```
F-NTFY-1: 복약 알림 (시간 설정, 반복 설정)
F-NTFY-2: 알림 액션 (복용완료, 빈도 줄이기)
F-DIET-1: 식단 기록 (음식 검색 + 칼로리)
F-GOAL-1: 목표 설정 (체중감량, 혈당 관리)
F-TRACK: 습관 추천 및 일일 기록
```

**Should Have (높은 우선순위)**
```
F-NTFY-3: 알림 미루기(스누즈)
F-NTFY-6: 라이프스타일 팁 커스터마이징
F-GOAL-2: AI 기반 식단/운동 가이드
F-REPORT: 주간 AI 리포트
```

**Could Have (낮은 우선순위)**
```
F-FAMILY: 가족 계정 추가
F-SHARE: 의료진과 정보 공유
F-VIDEO: 운동 가이드 영상
```

**Won't Have (차후 고려)**
```
블루투스 연동 (의료기기 승인 필요)
SNS 공유 (어르신 필요도 낮음)
```

#### TASK 정의 예시
```markdown
## T-NTFY-1: 복약 알림 UI 구현

### Description
사용자가 등록한 약물에 대해 설정된 시간에 푸시 알림 전송

### Acceptance Criteria
- [ ] 약물 시간 설정 화면에서 시간 선택 가능
- [ ] 설정된 시간에 정확히 알림 수신 (±1분 오차)
- [ ] 반복 설정(매일/특정요일) 동작 확인
- [ ] 오프라인 상태에서도 알림 수신 가능 (FCM)

### Technical Notes
- FCM (Firebase Cloud Messaging) 사용
- 로컬 타임존 기준 (UTC 변환 필수)
- push_scheduler 스케줄러로 매분 체크

### Estimation
- 백엔드: 8시간 (스케줄러 + API + 테스트)
- 프론트: 6시간 (UI + 상태관리 + 테스트)
- 총: 3일
```

### 2.4 Figma 화면설계

#### 설계 원칙
1. **Simple is Best**: 어르신도 1분 내에 이해할 수 있어야 함
2. **큰 터치 영역**: 최소 44px × 44px (모바일 접근성)
3. **명확한 색상**: 파스텔 핑크 + 어두운 회색 (대비 명확)
4. **글씨 크기**: 본문 최소 14px, 제목 20px+

#### 주요 화면 14개

| # | 화면명 | 담당 기능 | 디자인 특징 |
|---|---|---|---|
| 1 | 온보딩 | 회원가입 / 로그인 | 스텝 3개로 단순화 |
| 2 | 홈 | 오늘 약 알림 / 습관 추천 | 카드형 버튼, 한눈에 보이는 레이아웃 |
| 3 | 복약 스케줄 | 약 시간/반복 설정 | 시간 피커 (스크롤 쉽게) |
| 4 | 식단 기록 | 음식 검색 / 칼로리 누적 | 검색창 + 최근 기록 빠른 선택 |
| 5 | 운동 기록 | 운동 종류 / 시간 입력 | 드롭다운 + 숫자 입력 |
| 6 | 목표 설정 | 목표 생성 / 진행률 보기 | 진행률 바 + 상세 통계 |
| 7 | 습관 선택 | 질병별 추천 습관 | 질병별 배지 + 체크박스 |
| 8 | 마이다이어리 | 일일 기록 통합 | 탭 인터페이스 (식단/운동/일기) |
| 9 | 주간 리포트 | 주간 요약 / AI 분석 | 그래프 + 텍스트 분석 |
| 10 | 개인건강정보 | 키/몸무게/질병 입력 | 입력폼 + 저장 확인 |
| 11 | 알림함 | 모든 알림 모아보기 | 시간 역순, 읽음 표시 |
| 12 | 알림설정 | 시간대 / 빈도 커스터마이징 | 토글 + 슬라이더 |
| 13 | 응급안내 | 병원/약국 위치 검색 | 지도 + 1클릭 전화 |
| 14 | 설정 | 프로필 / 로그아웃 | 간단한 리스트 |

#### 특히 신경 쓴 부분

**홈 화면** (사용자 첫 진입)
```
┌─────────────────────────────────┐
│ 👋 홍길동 님, 안녕하세요!      │
├─────────────────────────────────┤
│                                 │
│ 🔔 다음 약: 타이레놀 09:30     │ ← 큰 텍스트
│   [복용완료] [30분 후]          │
│                                 │
│ 📅 오늘 할 일                  │
│  [식사] [운동] [수면] [습관]   │ ← 4개 칸
│                                 │
│ 💡 추천                        │
│  [추천 습관 보러가기]           │
│                                 │
└─────────────────────────────────┘
```

**복약 스케줄** (약 시간 설정)
```
현재 시간: 09:30

┌─────────────────────────────────┐
│ 타이레놀 500mg                  │
├─────────────────────────────────┤
│ 복용 시간:                      │
│  [시간]  [분]                  │
│  [06]   [30]  ← 큰 버튼        │
│  ↕️ (스크롤로 선택)             │
│                                 │
│ 반복:                           │
│  ○ 매일 ✓                      │
│  ○ 특정 요일                   │
│                                 │
│ [저장하기]                      │
└─────────────────────────────────┘
```

---

## 3. 개발 단계 (12주 담당)

### 3.1 아키텍처 설계

```
┌─────────────────────────────────────────────────┐
│                  Frontend (React)                │
│  - HomePage, DietLogContent, HabitSelection ... │
└──────────────────┬──────────────────────────────┘
                   │ REST API (FastAPI)
                   ↓
┌─────────────────────────────────────────────────┐
│               Backend (FastAPI)                  │
├─────────────────────────────────────────────────┤
│ Service Layer:                                  │
│  ├─ NotificationService (복약 알림)             │
│  ├─ DietService (식단 개인화)                    │
│  ├─ HabitService (습관 추천)                     │
│  ├─ GoalService (목표 관리)                      │
│  ├─ WeeklyReportService (주간 리포트)           │
│  └─ ContentNotificationService (라이프 팁)     │
├─────────────────────────────────────────────────┤
│ Data Layer:                                     │
│  ├─ Repository (CRUD 추상화)                   │
│  ├─ SQLAlchemy ORM (MyS QL)                     │
│  └─ Redis (캐싱)                               │
├─────────────────────────────────────────────────┤
│ External Services:                              │
│  ├─ AIWorkerGateway (Claude API)                │
│  ├─ 식약처 API (음식 영양정보)                  │
│  └─ FCM (푸시 알림)                             │
└─────────────────────────────────────────────────┘
```

### 3.2 핵심 기능별 구현

#### 1️⃣ 알림 시스템 (6개 PR)

**PR #280: 복용완료 액션 + 스마트 묶음**
```python
# 문제: 기존엔 액션이 "30분 후"만 있었음
# 해결: 복용완료 + 빈도줄이기 2개 액션 추가

class PushNotification:
    title: str
    body: str
    actions: list[Action]  # 최대 2개 (브라우저 제약)
    # - action.id="mark-taken" → medication_intake_logs에 즉시 기록
    # - action.id="reduce-frequency" → 알림 비활성화

# 같은 시각 알림 묶음
def send_scheduled_notifications():
    # 같은 프로필/같은 분(minute)의 알림 감지
    grouped = defaultdict(list)
    for alarm in pending_alarms:
        key = (alarm.profile_id, alarm.scheduled_at.minute)
        grouped[key].append(alarm)
    
    for key, alarms in grouped.items():
        # 여러 약을 하나의 메시지로 통합
        if len(alarms) > 1:
            titles = [a.medication_name for a in alarms]
            message = ", ".join(titles) + " 드실 시간이에요!"
        else:
            message = alarms[0].medication_name + " 드실 시간이에요!"
        
        send_push(profile_id=key[0], message=message, actions=STANDARD_ACTIONS)
```

**PR #323: 스누즈 (미루기)**
```python
# 문제: 지금 못 먹는데 미룰 방법이 없었음
# 해결: "30분 후에", "1시간 후에" 바텀시트 UI

class SnoozeReminder(Base):
    medication_id: int
    snooze_until: datetime  # now + 30min 또는 now + 1hour
    created_at: datetime

# 동작:
# 1) 사용자가 알림 본문 탭 → 바텀시트 열림
# 2) "30분 후에" 선택 → snooze_reminders에 저장
# 3) push_scheduler 매분 틱마다 확인
# 4) 시각 경과 → 자동 재알림 발송 → 기록 삭제
```

**PR #299 & #302: 알림함 + 딥링크**
```python
# 모든 알림을 한 곳에서 관리 + 클릭 시 관련 페이지로 이동

class NotificationLog(Base):
    profile_id: int
    title: str           # "타이레놀 복용 시간"
    body: str            # "지금 복용하세요"
    link_url: str        # "/alarms" 또는 "/chat" 등
    read_at: datetime    # NULL이면 미읽음

# PushService 한 곳의 훅으로 모든 알림 자동 저장
class PushService:
    @staticmethod
    def send_to_profile(
        profile_id: int,
        title: str,
        body: str,
        link_url: str = None,  # 옵션
        actions: list = None
    ):
        # 1) FCM/웹푸시 발송
        send_fcm(...)
        
        # 2) notification_logs에 저장 (자동)
        notification = NotificationLog(
            profile_id=profile_id,
            title=title,
            body=body,
            link_url=link_url
        )
        db.add(notification)
        db.commit()

# 프론트: 알림 클릭
const handleClick = (notification) => {
  if (notification.link_url) {
    navigate(notification.link_url);  // "/alarms", "/chat" 등
  }
};
```

**PR #282: 라이프스타일 팁 커스터마이징**
```python
# 기존: 무음 시간대만 있었음
# 개선: 팁 알림만 따로 시간대 + 빈도 설정

class Profile:
    # 기존 무음 시간대
    quiet_hours_start: time    # 22:00
    quiet_hours_end: time      # 07:00
    
    # 추가: 팁 알림 전용
    lifestyle_tip_enabled: bool = False
    lifestyle_tip_window_enabled: bool = False
    lifestyle_tip_start: time = time(9, 0)   # 기본값
    lifestyle_tip_end: time = time(21, 0)
    lifestyle_tip_min_interval_days: int = 1  # 1일, 3일, 7일 중 선택
    lifestyle_tip_last_sent_at: datetime = None

# 발송 전 체크
def should_send_lifestyle_tip(profile):
    # 체크 1: 팁 알림이 활성화되어 있는가?
    if not profile.lifestyle_tip_enabled:
        return False
    
    # 체크 2: 무음 시간대는 아닌가?
    now = datetime.now().time()
    if profile.quiet_hours_start < now < profile.quiet_hours_end:
        return False
    
    # 체크 3: 팁 전용 시간대가 활성화되어 있는가?
    if profile.lifestyle_tip_window_enabled:
        if not (profile.lifestyle_tip_start <= now <= profile.lifestyle_tip_end):
            return False
    
    # 체크 4: 빈도 간격이 경과했는가?
    if profile.lifestyle_tip_last_sent_at:
        days_elapsed = (now.date() - profile.lifestyle_tip_last_sent_at.date()).days
        if days_elapsed < profile.lifestyle_tip_min_interval_days:
            return False
    
    return True
```

#### 2️⃣ 식단 개인화 (1개 PR)

**PR #328: Mifflin-St Jeor 칼로리 계산 + AI 이유 생성**

```python
# 문제: 모든 사용자에게 일반적인 2,000kcal만 제시
# 해결: 키/몸무게/성별/나이 기반 개인화

def _compute_reference_kcal(health_profile) -> tuple[int, bool]:
    """
    Returns: (reference_kcal, is_personalized)
    """
    if not (health_profile and 
            health_profile.height_cm and 
            health_profile.weight_kg and
            health_profile.gender and
            health_profile.birth_date):
        return (2000, False)  # 폴백
    
    # Mifflin-St Jeor 공식
    h = health_profile.height_cm
    w = health_profile.weight_kg
    age = (date.today() - health_profile.birth_date).days // 365
    
    # BMR = 10*W + 6.25*H - 5*AGE + GENDER_CONST
    if health_profile.gender == Gender.MALE:
        bmr = 10*w + 6.25*h - 5*age + 5
    else:
        bmr = 10*w + 6.25*h - 5*age - 161
    
    # 활동 계수 1.375 (일반인)
    # 50kcal 단위로 반올림
    reference_kcal = round(bmr * 1.375 / 50) * 50
    
    return (reference_kcal, True)

# AI 기반 한 줄 이유 생성
def _get_kcal_reason(profile_id, log_date, reference_kcal, personalized):
    # 1) DB 캐시 확인 (profile_id, log_date 유니크)
    cached = repo.get_kcal_reason(profile_id, log_date)
    if cached:
        return cached['reason']
    
    # 2) AI Worker 호출
    health = profile.health_profile
    ai_reason = ai_worker.call(
        prompt=f"""
        사용자: 키 {health.height_cm}cm, 몸무게 {health.weight_kg}kg
        일일 권장 칼로리: {reference_kcal}kcal
        
        이 수치에 대해 의료적으로 정확한 한 줄 이유를 생성해주세요.
        (한국어, 50자 이내)
        """,
        schema=DietKcalReasonSummary
    )
    
    # 3) DB에 저장 (중복 저장 방지)
    repo.save_kcal_reason(profile_id, log_date, reference_kcal, ai_reason)
    
    return ai_reason

# 폴백 텍스트 (AI 실패 또는 데이터 미입력)
def _fallback_kcal_reason(personalized, reference_kcal, health):
    if personalized:
        return f"키 {health.height_cm}cm, 몸무게 {health.weight_kg}kg을 기준으로 계산되었습니다."
    else:
        return "일반 성인 기준 일일 권장 섭취량입니다."
```

**식단 기록 UI 게이팅**
```typescript
// DietLogContent.tsx
const [missingBodyInfo, setMissingBodyInfo] = useState(false);

useEffect(() => {
  healthInfoApi.get().then(info => {
    const missing = !info.height_cm || !info.weight_kg;
    setMissingBodyInfo(missing);
  });
}, []);

if (missingBodyInfo) {
  return (
    <Box>
      <Box sx={{ textAlign: 'center', py: 4 }}>
        <Typography variant="h4">📏</Typography>
        <Typography>키/몸무게를 먼저 입력해주세요</Typography>
      </Box>
      <Button
        fullWidth
        onClick={() => navigate('/health-info')}
      >
        건강정보 입력하기
      </Button>
    </Box>
  );
}

// 정상 식단 기록 UI
return <DietForm reference_kcal={reference_kcal} reason={kcal_reason} />;
```

#### 3️⃣ 습관 추천 (1개 PR)

**PR #328: 질병별 우선순위**
```python
# 문제: 질병 등록했는데도 기본 습관만 추천됨
# 해결: 질병 관련 습관을 먼저 노출

class HabitDef:
    key: str
    label: str
    is_disease_related: bool = False  # 플래그 추가

DISEASE_HABITS = {
    Disease.DIABETES: HabitDef(
        key="diabetes_check",
        label="혈당 체크하기",
        icon="🩺",
        is_disease_related=True
    ),
    Disease.HYPERTENSION: HabitDef(
        key="low_salt_diet",
        label="저염식 식사하기",
        icon="🧂",
        is_disease_related=True
    ),
    # ... 5개 질환별 1개씩
}

def pick_recommendations(profile_id, max_count=5):
    """
    질병 관련 습관을 먼저, 그 다음 기본 습관
    매일 같은 습관을 추천 (결정론적 로테이션)
    """
    pool = get_available_habits(profile_id)
    
    # 1단계: 질병별 분류
    disease_habits = [h for h in pool if h.is_disease_related]
    base_habits = [h for h in pool if not h.is_disease_related]
    
    # 2단계: 각 그룹 내 로테이션
    selected = []
    for habit_list in [disease_habits, base_habits]:
        if not habit_list:
            continue
        # 결정론적: (오늘 날짜 + 사용자ID) % 리스트길이
        rotation_idx = (today.toordinal() + profile_id) % len(habit_list)
        selected.append(habit_list[rotation_idx])
    
    return selected[:max_count]

# 예시: 당뇨 환자
# pick_recommendations(profile_id=123, date=2026-07-30)
# → [당뇨체크_습관, 기본습관_로테이션]
```

#### 4️⃣ 목표 관리 (2개 PR)

**PR #300: F-GOAL 1/2/3 전체**
```python
# F-GOAL-1: 목표 CRUD
class Goal(Base):
    profile_id: int
    title: str              # "체중 60kg 달성"
    goal_value: float       # 60
    current_value: float    # 75 (시작 시점)
    unit: str               # "kg"
    goal_type: str          # "NUMERIC" 또는 "FREQUENCY"
    period: str             # "SHORT_TERM"(≤31일) 또는 "LONG_TERM"
    created_at: datetime
    updated_at: datetime

# F-GOAL-2: AI 가이드 생성
def generate_guide_for_goal(goal: Goal, profile: Profile):
    """
    목표 + 진단질병 기반 AI 생성 가이드
    """
    ai_guide = ai_worker.call(
        prompt=f"""
        목표: {goal.title}
        진단질병: {', '.join([d.value for d in profile.diagnosed_diseases])}
        기간: {goal.period}
        
        이를 위해 필요한 식단 가이드와 운동 가이드를 생성해주세요.
        """,
        schema=DietExerciseGuide
    )
    
    # 캐싱 (수정할 때마다 재생성, 매일 재생성 X)
    goal.ai_guide_content = ai_guide.guide_text
    goal.updated_at = now()
    db.commit()

# F-GOAL-3: 일일 진행률 기록
class GoalProgressLog(Base):
    goal_id: int
    log_date: date
    current_value: float  # 그 날의 값 (예: 70kg)
    created_at: datetime

# 진행률 계산
def calculate_progress(goal: Goal, current_value: float) -> float:
    """
    진행률 = (현재값 - 시작값) / (목표값 - 시작값) × 100%
    """
    if goal.current_value == goal.goal_value:
        return 0
    
    progress = abs(current_value - goal.current_value) / \
               abs(goal.goal_value - goal.current_value)
    return min(progress * 100, 100)
```

**PR #320: 목표 타입 구분**
```python
# 기존: 모든 목표가 수치형 (체중, BMI)
# 개선: NUMERIC(수치형) / FREQUENCY(횟수형)

class Goal:
    goal_type: Enum  # "NUMERIC" 또는 "FREQUENCY"

# NUMERIC 예: "체중 60kg 달성"
# 기록: 매일 현재 체중 입력 → 진행률 자동 계산

# FREQUENCY 예: "주 5일 운동하기"
# 기록: [오늘 완료] 버튼 1회 클릭 → +1 (당일 중복 방지)
```

#### 5️⃣ 수면 기록 (1개 PR)

**PR #316: 수면 기록 + 홈 위젯**
```python
class SleepLog(Base):
    profile_id: int
    log_date: date
    sleep_hours: float     # 예: 7.5
    sleep_quality: int     # 1~5 (1=거의못잤음, 5=매우잘잤음)
    reason: str            # 품질 2점 이하일 때만 입력
    created_at: datetime

# 특징: (profile_id, log_date) 유니크 → 하루 1건만

# 홈 위젯
def get_health_indicators(profile_id, date):
    diet_log = get_diet_log(profile_id, date)
    exercise_log = get_exercise_log(profile_id, date)
    sleep_log = get_sleep_log(profile_id, date)
    
    # 참고값 (고정 또는 개인화)
    diet_reference = 2300  # kcal
    exercise_reference = 30  # minutes (WHO 권장)
    sleep_reference = 8  # hours
    
    return {
        "diet": {
            "current": diet_log.total_kcal,
            "reference": diet_reference,
            "progress": (diet_log.total_kcal / diet_reference) * 100
        },
        "exercise": {
            "current": exercise_log.total_minutes,
            "reference": exercise_reference,
            "progress": (exercise_log.total_minutes / exercise_reference) * 100
        },
        "sleep": {
            "current": sleep_log.sleep_hours,
            "reference": sleep_reference,
            "progress": (sleep_log.sleep_hours / sleep_reference) * 100
        }
    }
```

#### 6️⃣ 다이어리 (2개 PR)

**PR #291: 식단 + 운동 + 일기 + 주간 리포트**

```python
# 식단 기록
class DietLog(Base):
    profile_id: int
    log_date: date
    food_name: str       # "계란 계란말이"
    quantity: float      # 100 (그램)
    kcal: float
    protein_g: float
    carb_g: float
    fat_g: float

# 운동 기록 (MET 공식)
class ExerciseLog(Base):
    profile_id: int
    log_date: date
    exercise_type: str   # "달리기", "걷기", "줄넘기"
    duration_minutes: int
    intensity: str       # "저", "중", "고"
    distance_km: float   # 달리기/걷기만
    kcal_burned: float   # MET × 체중 × 시간

# 일기 (오늘의 한 줄)
class DiaryEntry(Base):
    profile_id: int
    log_date: date
    text: str            # "오늘 하루 좋았다~"
    photo_base64: str    # 사진 1장 (압축)

# 주간 AI 리포트
class WeeklyReport(Base):
    profile_id: int
    week_start: date
    report_content: str  # AI 생성 텍스트
    created_at: datetime

# 발송 (매주 일요일 9시)
def generate_weekly_report():
    """
    주 전체 식단/운동/수면/목표/습관 데이터를 수집
    AI로 요약 → 사용자에게 알림
    """
    profile = get_profile(profile_id)
    week_data = {
        "diet": aggregate_diet_logs(profile_id, week_start),
        "exercise": aggregate_exercise_logs(profile_id, week_start),
        "sleep": aggregate_sleep_logs(profile_id, week_start),
        "goals": get_goal_progress(profile_id, week_start),
        "habits": get_habit_completion(profile_id, week_start),
    }
    
    report = ai_worker.call(
        prompt=f"""
        이번 주 건강 데이터:
        - 식단: 평균 {week_data['diet']['avg_kcal']}kcal
        - 운동: 총 {week_data['exercise']['total_minutes']}분
        - 수면: 평균 {week_data['sleep']['avg_hours']}시간
        - 목표: {week_data['goals']}
        - 습관: {week_data['habits']}
        
        종합적인 한주 건강 분석을 작성해주세요.
        """,
        schema=WeeklyReportOutput
    )
    
    # 저장 + 알림 발송
    report_obj = WeeklyReport(
        profile_id=profile_id,
        week_start=week_start,
        report_content=report.content
    )
    db.add(report_obj)
    db.commit()
    
    PushService.send_to_profile(
        profile_id=profile_id,
        title="이번 주 건강 리포트 도착",
        body="주간 복약순응도 및 생활습관 분석",
        link_url="/weekly-reports"
    )
```

#### 7️⃣ UI 통일화 (1개 PR)

**PR #305: 제목 아이콘 + 버튼 모서리 표준화**

```typescript
// PageTitle.tsx (새로 추출한 컴포넌트)
interface PageTitleProps {
  icon: React.ReactNode;
  children: string;
}

export default function PageTitle({ icon, children }: PageTitleProps) {
  return (
    <Box sx={{ display: 'flex', alignItems: 'center', gap: 2, mb: 3 }}>
      <Box
        sx={{
          width: 36,
          height: 36,
          borderRadius: '50%',
          backgroundColor: '#FFE4F0',  // pageBg
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: '#FF6F91',
        }}
      >
        {icon}
      </Box>
      <Typography variant="h4" sx={{ fontWeight: 700 }}>
        {children}
      </Typography>
    </Box>
  );
}

// 사용 예
<PageTitle icon={<Calendar size={20} />}>복약스케줄</PageTitle>

// 버튼 모서리 표준화
// 기본: borderRadius: 10
// 카드: borderRadius: 16
// 토글: borderRadius: 999
```

---

## 4. 핵심 기술 & 의사결정

### 4.1 AI 캐싱 전략

**문제**: AI Worker 호출은 비용이 많이 들고 느림

**해결**:
```python
# (profile_id, log_date) 유니크 제약
# → 같은 날 같은 사용자는 AI를 1회만 호출
# → 그 다음부터는 DB 캐시에서 조회

class DietKcalReason(Base):
    profile_id: int
    log_date: date
    reference_kcal: int
    reason: str
    
    __table_args__ = (
        UniqueConstraint('profile_id', 'log_date'),
    )

# 효과
# 일일 1회 → 평균 1/5 비용 절감 (사용자가 하루에 여러 번 조회해도)
```

### 4.2 폴백 설계

**원칙**: AI 실패해도 서비스는 중단되면 안 됨

```python
def get_kcal_reason(profile_id, log_date, reference_kcal, personalized):
    try:
        # 1) 캐시 확인
        cached = repo.get_kcal_reason(profile_id, log_date)
        if cached:
            return cached['reason']
        
        # 2) AI 호출
        ai_reason = ai_worker.call(...)
        repo.save_kcal_reason(profile_id, log_date, reference_kcal, ai_reason)
        return ai_reason
        
    except AIWorkerException:
        # 3) AI 실패 → 폴백
        return _fallback_kcal_reason(personalized, reference_kcal, health)

def _fallback_kcal_reason(personalized, reference_kcal, health):
    if personalized:
        return f"키 {health.height_cm}cm, 몸무게 {health.weight_kg}kg을 기준으로 합니다."
    else:
        return "일반 성인 기준 일일 권장 섭취량입니다."
```

### 4.3 PushService 단일 훅

**문제**: 알림 종류마다 코드 중복 (복약 알림, 리포트, 팁 등)

**해결**:
```python
class PushService:
    @staticmethod
    def send_to_profile(
        profile_id: int,
        title: str,
        body: str,
        link_url: str = None,
        actions: list = None
    ):
        # 한 곳에서 모든 알림 처리
        # 1) FCM/웹푸시 발송
        # 2) notification_logs 자동 저장
        # 3) 사용자 설정(무음 시간대) 확인
        # 4) 구독 상태 체크
```

**효과**: 새 알림 기능 추가 시 `PushService.send_to_profile()` 호출만 하면 끝

---

## 5. 성과 및 배운 점

### 5.1 정량적 성과

| 지표 | 수치 |
|---|---|
| 개발 PR | 15개 |
| 마이그레이션 | 15개 (무중단 배포) |
| 신규 기능 | 16개 (알림 6 + 개인화 9 + UI 1) |
| 테스트 | 20+ 신규 (회귀 0개) |
| 코드 라인 | 4,000+ 변경 |
| 품질 체크 | ruff/mypy/tsc/eslint 100% 통과 |

### 5.2 정성적 성과

**기획 단계의 가치**
- 벤치마킹 → 차별화 포인트 명확화 ("간단함" 강조)
- 요구사항정의 → 개발 중 스코프 크리프 차단
- 화면설계 → 개발자와 소통 비용 50% 절감
- API 서치 → 불필요한 외부 의존성 제거

**개발 단계의 가치**
- AI 캐싱 전략 → 비용 효율 및 응답속도 개선
- 폴백 설계 → 안정성 100% (AI 실패해도 서비스 유지)
- 단일 훅 (PushService) → 확장성 극대화
- 테스트 기반 개발 → 리팩토링 자신감 ↑

### 5.3 배운 점

**기술적 배운 점**
1. **마이그레이션 전략**: 무중단 배포의 중요성 (ALTER TABLE 순서 중요)
2. **캐싱의 진정한 가치**: 단순 속도 향상이 아니라 비용 절감 + UX 개선
3. **폴백 설계의 필요성**: 모든 외부 서비스는 실패 가능하다는 전제
4. **데이터 정합성**: HealthProfile 분리로 PII 관리 및 확장 용이

**협업 배운 점**
1. **벤치마킹의 진정한 목적**: 경쟁사 따라하기 아니라 "우리만의 강점" 찾기
2. **요구사항정의의 가치**: 개발 중 혼동과 재작업을 50% 줄임
3. **화면설계 → 개발의 매끄러운 전환**: Figma 상세도가 높을수록 코드 리뷰가 쉬움
4. **PR 리뷰**: 전체 맥락을 이해하는 작은 PR이 거대한 PR보다 리뷰하기 훨씬 좋음

**어르신 사용자를 고려한 설계**
1. 아름답지만 복잡한 UI보다 **심플하고 명확한 UI**가 우선
2. 터치 영역 44px × 44px는 선택이 아니라 필수
3. 큰 폰트 + 높은 명암 + 적절한 여백 = 최고의 UX
4. 온보딩 단계는 3개 이하 (어르신들은 5개 이상이면 포기)

---

## 6. 포트폴리오 링크 및 증거

### 깃허브 링크
- **전체 PR**: [#280 ~ #328](https://github.com/Ji-gam/AH_04_01/pulls?q=is%3Apr+author%3Aannajeong0429-ux)
- **최종 마이그레이션**: 0044~0058 (15개)
- **테스트 커버리지**: app/tests/services, app/tests/habit_apis, app/tests/goal_apis

### 시연 영상 (예상)
- 복약 알림부터 주간 리포트까지 6~8분 시나리오
- 실제 기능 동작 라이브 데모
- 어르신 사용성 평가 피드백

### 기타 증거
- **기획 문서**: 벤치마킹 보고서, 요구사항정의서, Figma 화면설계 (링크)
- **발표 자료**: 최종 발표 PPT (24 슬라이드)
- **코드 리뷰**: 팀원들의 PR 코멘트 및 승인

---

## 7. 이력서에 넣을 원라이너 버전

### 한 줄 요약
```
"50~70대 어르신을 위한 개인화 건강관리 플랫폼 ReMedi 개발:
벤치마킹부터 배포까지 전 과정 담당 (기획 2주 + 개발 12주),
15개 PR, 4,000+ 라인 코드, 회귀 0개, 모든 품질 체크 통과"
```

### 역할별 한 줄
```
기획: "닥터다이어리/나의건강정보기록 벤치마킹 → 우리만의 차별화점 도출 (간단함)"
설계: "Figma 14개 화면 설계, 어르신 사용성 중심 (44px 터치 + 큰 폰트)"
개발: "알림(6개 PR) + 개인화(9개 PR) 풀스택 구현, AI 캐싱 전략 (비용 1/5)"
품질: "마이그레이션 15개 무중단 배포, 테스트 20+, ruff/mypy/tsc/eslint 100%"
```

---

## 8. 이력서 포맷 (한국 일반형)

```
【프로젝트명】ReMedi: 개인화 건강관리 플랫폼

【기간】2026.04 ~ 2026.07 (4개월, 총 16주)

【팀 규모】8명 (기획 1 / 개발 5 / PM 1)

【담당 역할】
- 기획: 벤치마킹, 요구사항정의서 작성, Figma 화면설계 (14개 화면)
- 백엔드: FastAPI 기반 알림/식단/습관/목표/수면/다이어리 API 구현
- 프론트엔드: React 기반 UI 구현, 상태 관리, 타입스크립트

【주요 성과】
- 개발 PR 15개 (회귀 0개), 마이그레이션 15개 무중단 배포
- AI 캐싱 전략으로 비용 1/5 절감 + 응답속도 개선
- 16개 신규 기능 (알림 6 + 개인화 9 + UI 1)
- ruff/mypy/tsc/eslint 100% 통과

【기술 스택】
- Backend: Python/FastAPI, SQLAlchemy, MySQL, Redis, Claude API
- Frontend: TypeScript/React, TanStack Query, Zustand, Tailwind CSS

【주요 기술】
- Mifflin-St Jeor 공식 기반 개인화 칼로리 계산
- (profile_id, log_date) 유니크 AI 결과 캐싱
- 질병별 맞춤 습관 우선순위 로직
- PushService 단일 훅으로 모든 알림 타입 커버
```

---

## 9. 포토폴리오 사이트에 넣을 텍스트

```html
<article class="project">
  <h2>ReMedi: 개인화 건강관리 플랫폼</h2>
  
  <section class="overview">
    <h3>프로젝트 소개</h3>
    <p>
      50~70대 어르신을 위한 개인화 건강관리 플랫폼입니다. 
      복약 알림, 식단·운동·수면 기록, AI 기반 주간 리포트, 
      질병별 맞춤 생활습관 추천을 제공합니다.
    </p>
    <p>
      사용자 특성을 고려해 '간단함'을 최우선으로 설계했습니다.
      기획 단계부터 개발·배포까지 전 과정을 담당했습니다.
    </p>
  </section>
  
  <section class="role">
    <h3>담당 역할</h3>
    <ul>
      <li><strong>기획</strong>: 벤치마킹(닥터다이어리/나의건강정보기록), 
          요구사항정의서 작성, Figma 화면설계 14개</li>
      <li><strong>백엔드</strong>: FastAPI 기반 API 구현 (15개 PR)</li>
      <li><strong>프론트엔드</strong>: React 기반 UI 구현, 상태관리</li>
      <li><strong>인프라</strong>: 마이그레이션 전략, 무중단 배포</li>
    </ul>
  </section>
  
  <section class="achievements">
    <h3>주요 성과</h3>
    <table>
      <tr><td>PR 개수</td><td>15개 (회귀 0개)</td></tr>
      <tr><td>마이그레이션</td><td>15개 무중단 배포</td></tr>
      <tr><td>신규 기능</td><td>16개</td></tr>
      <tr><td>테스트</td><td>20+ 신규</td></tr>
      <tr><td>코드 변경</td><td>4,000+ 라인</td></tr>
      <tr><td>품질 체크</td><td>100% 통과</td></tr>
    </table>
  </section>
  
  <section class="tech-highlights">
    <h3>핵심 기술</h3>
    <dl>
      <dt>AI 캐싱</dt>
      <dd>(profile_id, log_date) 유니크 제약으로 AI 호출 1/5 비용 절감</dd>
      <dt>폴백 설계</dt>
      <dd>AI 실패 시에도 서비스 중단 없이 규칙 기반 텍스트 제공</dd>
      <dt>개인화 칼로리</dt>
      <dd>Mifflin-St Jeor 공식으로 사용자별 맞춤 권장량 계산</dd>
      <dt>습관 우선순위</dt>
      <dd>질병 관련 습관을 먼저 노출, 결정론적 로테이션</dd>
    </dl>
  </section>
  
  <section class="tech-stack">
    <h3>기술 스택</h3>
    <p><strong>Backend</strong>: Python 3.11, FastAPI, SQLAlchemy, MySQL, Redis, Claude API</p>
    <p><strong>Frontend</strong>: TypeScript, React 18, TanStack Query, Zustand, Tailwind CSS</p>
    <p><strong>DevOps</strong>: Alembic 마이그레이션, Ruff/MyPy, pytest, ESLint/Prettier</p>
  </section>
  
  <section class="links">
    <h3>관련 링크</h3>
    <ul>
      <li><a href="https://github.com/Ji-gam/AH_04_01">GitHub 저장소</a></li>
      <li><a href="#figma-design">Figma 설계 화면</a></li>
      <li><a href="#demo-video">시연 영상</a></li>
    </ul>
  </section>
</article>
```

---

## 마지막 팁: 면접 질문 대비

### Q1. "기획에서 개발까지 전체를 했다고 했는데, 구체적으로 뭐를 했어요?"

**답변**:
```
크게 3가지 단계로 나눌 수 있습니다.

1️⃣ 기획 (2주)
- 닥터다이어리와 나의건강정보기록을 분석했고,
  어르신들은 "간단함"을 원한다는 인사이트를 얻었습니다.
- 그래서 우리는 기능을 5개 이내로 단순화했습니다.
- Figma로 14개 화면을 설계했는데, 터치 영역 44px + 큰 폰트를 
  최우선으로 했습니다.

2️⃣ 개발 (12주)
- 15개의 PR을 통해 알림 6개, 개인화 9개 기능을 구현했습니다.
- 특히 AI 캐싱 전략으로 비용을 1/5로 줄이면서도 
  100% 서비스 가용성을 보장했습니다.

3️⃣ 배포 (지속)
- 15개의 마이그레이션을 무중단 배포했습니다.
- 회귀 테스트 0개, 모든 품질 체크 100% 통과입니다.
```

### Q2. "어르신 사용자를 고려한 특별한 설계가 있었나요?"

**답변**:
```
세 가지가 있습니다.

1️⃣ 온보딩 단순화
- 기존: 5단계 이상 (회원가입 → 약 설정 → 질병 입력...)
- 개선: 3단계 (이메일/비번 → 약 1개 등록 → 끝)

2️⃣ UI/UX
- 터치 영역 최소 44px × 44px (모바일 접근성 표준)
- 본문 폰트 최소 14px (어르신 눈 고려)
- 주요 버튼은 큰 텍스트 (20px+)

3️⃣ 기능 설계
- "추천" 기능은 자동화 (어르신들이 선택 피로 느낌)
- "미루기(스누즈)" 추가 (30분 후, 1시간 후)
- 한눈에 보이는 대시보드 (복약할 약 + 오늘 할 일 4개)
```

### Q3. "기획과 개발을 동시에 했는데, 우선순위는 어떻게 정했어요?"

**답변**:
```
MoSCoW 방식을 썼습니다.

Must Have:
- 복약 알림 (필수)
- 식단 기록 (필수)
- 목표 설정 (필수)

Should Have:
- 스누즈 (높은 우선순위)
- AI 리포트 (높은 우선순위)

Could Have:
- 가족 계정 (낮은 우선순위)

Won't Have:
- 블루투스 의료기기 연동 (시간 부족)

이렇게 하니까 스코프 크리프가 없었고,
16주 안에 원래 계획한 대로 끝낼 수 있었습니다.
```

---

이렇게 정리하면 이력서/포토폴리오/면접에서 모두 활용 가능합니다! 🎉
