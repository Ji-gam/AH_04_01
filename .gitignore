__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
.*.env
.env
.idea/
.vscode
.coverage
*.coverage
.coverage.*
docker-compose.override.yml
# frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# oh-my-claudecode local state
.omc/

# chroma db data
ai_worker/chroma_data/

# macOS junk
.DS_Store

# local raw-data staging (source CSVs/DB for app/database/*.db builds)
_data/

# ai_worker/source/는 RAG+구조화 데이터의 단일 원천이라 통째로 추적하되, GitHub 파일
# 크기 한도(100MB 하드, 50MB 경고)를 넘는 두 개만 제외한다. 인제스트는 처리할 파일을
# 특정하지 않고 "있는 것만" 읽고 없으면 건너뛰므로(ingest.ingest_csv_data,
# ingest_papers.load_raw_papers), 이 둘이 없어도 파이프라인은 정상 동작한다.
# 필요한 사람은 식약처 공공데이터에서 직접 받는다.
ai_worker/source/dur_prod_usjnt_taboo.csv
ai_worker/source/drug_prdt_prmsn_detail.csv

# drug_info_sync pipeline outputs (regenerated by scripts/drug_info_sync)
app/database/drugs_full.db
scripts/drug_info_sync/progress_db.log
scripts/drug_info_sync/download_db_mode/

