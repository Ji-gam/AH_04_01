__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
.*.env
.env
.idea/
.vscode
.coverage
*.coverage
.coverage.*
docker-compose.override.yml
# frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# oh-my-claudecode local state
.omc/

# chroma db data
ai_worker/chroma_data/

# macOS junk
.DS_Store

# local raw-data staging (source CSVs/DB for app/database/*.db builds)
_data/

# ai_worker/source/는 RAG 드롭 폴더, ai_worker/source/_not_rag/는 SQL 조회용 구조화
# 데이터다. 둘 다 추적하되 GitHub 파일 크기 한도(100MB 하드, 50MB 경고)를 넘는 두 개만
# 제외한다. 경로를 정확히 적어야 한다 — 파일을 옮기면 이 규칙이 안 맞아서 662MB 파일이
# 추적 대상으로 노출되고 push가 거부된다.
# 필요한 사람은 식약처 공공데이터에서 직접 받는다.
ai_worker/source/_not_rag/dur_prod_usjnt_taboo.csv
ai_worker/source/_not_rag/drug_prdt_prmsn_detail.csv

# drug_info_sync pipeline outputs (regenerated by scripts/drug_info_sync)
app/database/drugs_full.db
scripts/drug_info_sync/progress_db.log
scripts/drug_info_sync/download_db_mode/

