__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
.*.env
.env
.idea/
.vscode
.coverage
*.coverage
.coverage.*
docker-compose.override.yml
# frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# oh-my-claudecode local state
.omc/

# chroma db data
ai_worker/chroma_data/

# macOS junk
.DS_Store

# local raw-data staging (source CSVs/DB for app/database/*.db builds)
_data/

# ai_worker/source/는 RAG 드롭 폴더다. 여기엔 RAG 재료만 둔다(현재 25MB 남짓).
# SQL 조회용 구조화 데이터는 여기 두지 않는다 — 아래 drug_info_sync가 받아 DB에 적재하고,
# 어떤 코드도 source/에서 그 CSV를 읽지 않는다. 한때 사본이 여기 있었으나(97MB) 지웠다.

# drug_info_sync pipeline outputs (regenerated by scripts/drug_info_sync)
app/database/drugs_full.db
scripts/drug_info_sync/progress_db.log
scripts/drug_info_sync/download_db_mode/

