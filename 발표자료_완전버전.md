# 발표자료 최종본 — ReMedi 개인화 건강관리 시스템 전체 기능 (annajeong0429-ux)

> 팀 프로젝트 결과보고서 템플릿(별첨2-2)의 **02(팀 구성 및 역할) / 03(수행 절차 및 방법) / 04(수행 경과)** 슬라이드에 대응하는 초안입니다.
> 모든 수치와 사실관계는 git log, PR, 마이그레이션, 테스트를 근거로 했습니다.

---

## 슬라이드 6 — 02. 프로젝트 팀 구성 및 역할

**담당: 알림 시스템 + 개인화 건강관리 시스템 전체**

### A. 알림 시스템 (6개 PR, 4개 마이그레이션)

- 복약 알림 액션 개선: 복용완료/빈도줄이기 (F-NTFY-1)
- 스마트 알림 묶음: 같은 시각 알림 통합 (F-NTFY-2)
- 알림 미루기(스누즈): 바텀시트 UI (F-NTFY-3)
- 라이프스타일 팁 커스터마이징: 시간대 + 수신 빈도 (F-NTFY-6)
- 알림함 통합: 모든 알림 한 곳에서 관리, 안읽음 배지
- 딥링크: 알림 클릭 → 관련 페이지로 즉시 이동
- 안드로이드 호환성: 푸시 수신/테스트 버튼 안정화

### B. 건강정보 기반 개인화 시스템

**① 식단 기록 개인화** (1개 PR, 2개 마이그레이션)
- 개인화 칼로리 계산: Mifflin-St Jeor 공식 (키/몸무게/성별/나이 기반)
- AI 기반 한 줄 이유 생성 + 캐싱 (daily 1회 호출로 비용 절감)
- 식단 기록 UI 게이팅: 건강정보 미입력 시 자동 유도
- 주간 리포트에 BMI 포함

**② 습관 추천 시스템** (2개 PR, 완전 재설계)
- 질병 등록 시 AI가 생성한 질병 맞춤 습관만 노출 (기본 습관 완전 배제)
- 3단계 폴백: 세부 진단 시드 습관 → AI 동적 생성 → 질병별 고정 폴백
- 다중 질병 시 질병당 최소 1개 배분 + 라벨 기준 중복 제거
- 습관별 질병 연관 추천 이유 세분화 생성 (6개 질병 × 키워드별 설명)
- AI 생성 대기 중 로딩 스피너 화면 추가

**③ 목표 관리** (2개 PR, 2개 마이그레이션)
- 목표 설정(F-GOAL-1): 수치형/횟수형 목표 CRUD
- AI 맞춤 가이드(F-GOAL-2): 목표+질병 기반 식단/운동 가이드 자동 생성
- 일일 진행률 기록: 매일 수치 업데이트 → 7일 추이 그래프
- 달성 리포트(F-GOAL-3): 주간/월간 진행률 반영

**④ 수면 기록** (1개 PR, 1개 마이그레이션)
- 수면 시간 + 수면의 질 5단계 기록
- 품질 저하 시 원인 입력칸 추가
- 홈 위젯: 식사/운동/수면 진행률 바 3칸 (개별 클릭 가능)
- 주간 AI 리포트에 평균 수면시간/질 반영

**⑤ 다이어리** (2개 PR, 6개 마이그레이션)
- 식단 기록(F-DIET-1/2): 식약처 DB 연동, 영양성분 분석
- 운동 기록: MET 공식 기반 소모 칼로리 계산
- 주간 AI 리포트: 매주 일요일 9시 자동 생성 (식단/운동/습관/복약 종합 분석)
- 오늘의 한 줄(일기): 1줄 + 사진 1장 기록

**⑥ UI/디자인 통일** (1개 PR)
- 제목 아이콘: 14개 화면 lucide 아이콘으로 통일
- 버튼 모서리: 기본 10px + 카드 16px + 토글 999px로 표준화
- 다크모드 팔레트 보정

**⑦ AI 피드백 루프** (1개 PR, 1개 마이그레이션)
- 습관 추천 이유·식단 칼로리 이유에 👍/👎 평가 기능 신설 (`reason_feedbacks` 테이블)
- 재평가 시 upsert, 두 기능이 서비스 계층을 공유하는 재사용 가능한 구조로 설계

**⑧ 식단·운동 기록 UX 개선** (1개 PR, 1개 신규 엔드포인트)
- 식단: 검색 결과 여러 개가 각자 인분선택+기록버튼을 가져 복잡하던 문제 → 목록 선택 후 하나만 설정하는 흐름으로 재설계
- 운동: 세부 종목 구분 없는 고정 23개 목록이라 자유검색 대신 드롭다운으로 전환
- 운동 "기타(직접 입력)": AI가 자유 입력 운동명의 MET(대사당량) 값을 추정해 칼로리 계산

### C. 기타 유지보수

- 주간 리포트 발송: 요일 통일 (일요일)
- 마이그레이션 충돌 해결: 0055 리비전 정리
- 표기 오류 수정: "복약스케쥴" → "복약스케줄"
- 알림함 딥링크 수정: 월간 달성 리포트 클릭 시 마이다이어리 대신 주간 리포트로 이동

*(다른 팀 부분: 위치정보 응급 안내, 약물 검색/등록, 약 상호작용 분석 등)*

---

## 슬라이드 7 — 03. 프로젝트 수행 절차 및 방법

**"기능 추가 → 문제 해결 → 사용성 개선"의 반복 순환**

### A. 알림 시스템 (완성도 향상 과정)

| 단계 | 활동 | 기간/PR |
|---|---|---|
| 기본 액션 | 복용완료/빈도줄이기 버튼 + 같은 시각 알림 묶음 | PR #280 |
| 사용자 경험 개선 | 스누즈(미루기) 바텀시트 추가 | PR #323 |
| 커스터마이징 | 라이프스타일 팁 전용 시간대+빈도 설정 | PR #282 |
| 발견성 강화 | 홈 상단 🔔 알림함 + 알림 클릭 → 관련 페이지 이동 | PR #299, #302 |
| 안정화 | 안드로이드 호환성 버그 수정 | PR #298 |

### B. 건강정보 개인화 시스템 (데이터 → 활용 → 분석)

| 구간 | 주요 작업 | 대응 PR |
|---|---|---|
| **기반 데이터 구축** | | |
| 1. 다이어리 기능 신설 | 식단/운동/일기 기록 + 주간 AI 리포트 | PR #291 |
| 2. 목표 관리 | 목표 CRUD + AI 가이드 + 진행률 기록 | PR #300 |
| 3. 수면 기록 | 수면 시간/질 기록 + 홈 위젯 | PR #316 |
| | | |
| **개인화 적용** | | |
| 1. 식단 개인화 | Mifflin-St Jeor 칼로리 + AI 이유 + 게이팅 | PR #328 |
| 2. 습관 우선순위 | 질병 관련 습관 먼저 노출 | PR #328 |
| 3. 주간 리포트 확장 | BMI + 목표진도 + 수면 통합 | PR #328, #301 |
| | | |
| **UI 통일** | 제목 아이콘 + 버튼 모서리 표준화 | PR #305 |
| **목표 개선** | 목표 타입 구분(횟수형/수치형) | PR #320 |
| | | |
| **사용자 피드백 확장** | | |
| 1. AI 피드백 루프 | 습관/식단 이유에 👍/👎 평가 신설 | PR #345 |
| 2. 식단 검색 UX | 결과 목록 → 선택 → 설정 흐름으로 재설계 | PR #346 |
| 3. 운동 기록 UX | 드롭다운 전환 + "기타" AI MET 추정 | PR #346 |

---

## 슬라이드 8 — 04. 프로젝트 수행 경과 (개요)

이어지는 슬라이드는 다음 3개 파트로 구성됩니다:

**1. 알림 시스템 파트 (4 슬라이드)**
   - ① 스마트 발송 (복용완료 액션 + 시간대 묶음)
   - ② 스누즈 & 커스터마이징 (미루기 + 팁 설정)
   - ③ 알림함 통합 (모든 알림 한 곳, 딥링크)
   - ④ 안정화 (안드로이드 호환성)

**2. 개인화 시스템 파트 (9 슬라이드)**
   - ① 다이어리 기반 (식단/운동/일기/AI 리포트)
   - ② 목표 관리 (F-GOAL 1/2/3)
   - ③ 수면 기록 (수면 시간/질 + 홈 위젯)
   - ④ 식단 개인화 (Mifflin-St Jeor + AI 이유)
   - ⑤ 습관 추천 (질병 특화 AI 생성)
   - ⑥ 주간 리포트 통합 (BMI/목표/수면)
   - ⑦ AI 피드백 루프 (습관/식단 이유 👍/👎)
   - ⑧ 식단 검색 UX 개선 (목록 선택 → 설정)
   - ⑨ 운동 기록 UX 개선 (드롭다운 + AI MET 추정)

**3. 설계 원칙**
   - UI 통일화
   - 목표 개선
   - 시연 시나리오

---

## 슬라이드 9 — A① 알림: 스마트 발송

**문제 → 해결: 복용완료 액션 + 시간대 묶음 (F-NTFY-1, F-NTFY-2)**

### 1. 복용완료 액션 추가 (F-NTFY-1)

```
푸시 알림 액션 버튼:
┌──────────────┬──────────────┐
│ 복용완료     │ 빈도 줄이기  │
└──────────────┴──────────────┘

→ "복용완료" 클릭 시:
   • medication_intake_logs에 즉시 기록
   • 약력에 반영 (복약순응도 ↑)
   • API: POST /push/mark-taken
```

### 2. 같은 시각 알림 묶음 (F-NTFY-2)

```
예시: 09:00에 약 2개

기존:
  09:00 - "타이레놀 드실 시간이에요!"
  09:00 - "종합비타민 드실 시간이에요!"
  → 2번 울림

개선:
  09:00 - "타이레놀정, 종합비타민 드실 시간이에요!"
  → 1번 울림 (배터리/사용자 피로도 ↓)

동작:
• push_scheduler가 같은 프로필/같은 분(minute)의 알림 감지
• 하나의 메시지로 통합 발송
• 액션 버튼은 전체에 적용 (둘 다 복용, 또는 둘 다 빈도 줄이기)
• 서비스워커가 각 항목별로 API 반복 실행
```

**기술 포인트**
- 마이그레이션 0047: 스케줄링 로직 최적화, 기존 snooze 코드 제거
- 테스트: 같은 시각 약 2개 등록 → 한 묶음 발송 확인

---

## 슬라이드 10 — A② 알림: 스누즈 & 커스터마이징

**문제 → 해결: 약 때때로 못 먹을 때 미루기 + 팁 알림 세밀 제어**

### 1. 스누즈: 알림 미루기 (F-NTFY-3)

```
복약 알림 받음:
  ↓ (제목 부분 탭)
인앱 바텀시트 열림:
  [ 30분 후에 ]
  [ 1시간 후에 ]
  ↓ (선택)
snooze_reminders 테이블에 예약 저장
  ↓ (30분 경과)
자동 재알림 발송 후 기록 삭제
```

**구현**
```python
class SnoozeReminder(Base):
    medication_id: int
    snooze_until: datetime  # now + 30min or now + 1hour
    created_at: datetime
```

- push_scheduler 매분 틱마다 확인
- 여러 번 스누즈 가능 (사용자 자율)

### 2. 라이프스타일 팁 커스터마이징 (F-NTFY-6)

기존 무음 시간대와 별개로, "팁 알림"만 세밀 제어:

```
설정 > 알림:
┌─────────────────────────────────────┐
│ 🔔 라이프스타일 팁 알림      [토글] │
├─────────────────────────────────────┤
│ ⚙️ 수신 시간대: 09:00 ~ 21:00    │
│                                     │
│ ⚙️ 수신 빈도:                      │
│    ○ 제한 없음                      │
│    ○ 하루에 한 번                  │
│    ○ 3일에 한 번    ← 선택됨       │
│    ○ 일주일에 한 번                │
└─────────────────────────────────────┘
```

**동작**
- 토글 OFF → 기존 무음 시간대 사용
- 토글 ON → 전용 시간대 + 빈도 검사
- lifestyle_tip_last_sent_at 추적
- 발송 전 두 조건 검사: ① 무음 시간대 밖? ② 빈도 간격 경과?

**마이그레이션**: 0037 (lifestyle_tip 전용 설정 칼럼)

---

## 슬라이드 11 — A③ 알림: 알림함 통합 & 딥링크

**문제 → 해결: 알림을 받아도 어디로 가야 할지 몰랐음 → 한 곳에서 관리 + 즉시 이동**

### 1. 홈 상단 알림함

```
홈 화면:
  [검색] [🔔 3] [⋯]
           ↑ 안 읽은 알림 배지

클릭 → 알림함 패널 열림:
┌────────────────────────────────┐
│ 알림함                         │
├────────────────────────────────┤
│ 📌 07:30                       │
│    타이레놀                    │
│    복용완료 버튼 누르기        │
│                                │
│ 📋 05:20                       │
│    이번 주 복약 정리           │
│    (주간 리포트)               │
│                                │
│ 🏥 어제                        │
│    공지사항 도착               │
└────────────────────────────────┘
```

**구현**
- 신규 `notification_logs` 테이블 (마이그레이션 0044)
- PushService.send_to_profile() **한 곳의 훅** → 모든 알림 자동 저장
- 웹푸시/FCM 구독 없어도 인앱 알림함에는 항상 남음 (신뢰성 ↑)

### 2. 딥링크: 알림 클릭 → 관련 페이지로 이동

```
notification_logs에 link_url 저장 (마이그레이션 0048)

알림 종류별 이동 경로:
┌──────────────────┬─────────────────┐
│ 복약 알림        │ /alarms         │
│ 주간 리포트      │ /weekly-reports │
│ 복약순응도 리포트│ /track          │
│ 습관 목표 달성   │ /habit-selection│
│ 공지사항         │ /notices        │
│ 챗봇 답변        │ /chat           │
│ 부작용 안내      │ /chat?...       │
└──────────────────┴─────────────────┘
```

**프론트 동작**
```typescript
const handleNotificationClick = (notification) => {
  if (notification.link_url) {
    closePanel();
    navigate(notification.link_url);  // 해당 페이지 즉시 이동
  }
};
```

**특징**
- link_url 없으면 기존 consult_url 자동 재사용 (호환성)
- 알림함 열 때 전체 "읽음" 처리

---

## 슬라이드 12 — A④ 알림: 안드로이드 호환성 안정화

**문제**: 안드로이드 Chrome에서 푸시 수신/테스트 버튼이 실패

**해결 (PR #298)**

```typescript
// Before (X): 안드로이드에서 예외
new Notification("테스트", { ... })

// After (O): 실제 푸시와 동일
registration.showNotification("테스트", { ... })
```

**개선 사항**
1. 테스트 알림: `registration.showNotification()` 사용
2. 실패 피드백: 알림 켜기 실패 시 에러 메시지 명확 표시

**테스트**
- npm 타입체크/빌드 통과
- 안드로이드 실기기 푸시 수신 확인

---

## 슬라이드 13 — B① 개인화: 다이어리 기능 (식단/운동/일기)

**목표**: 사용자의 일일 생활 데이터 수집 → 개인화 기반 구축

### 1. 식단 기록 (F-DIET-1/2)

```
더보기 > 마이다이어리 > 식단:
  1) 음식명 검색 (식약처 DB API 또는 로컬 시드)
  2) 섭취량 입력
  3) 저장 → 오늘 총 칼로리/영양성분 표시
  
화면 구성:
  • 오늘 섭취: 1,850kcal / 탄:100g / 단:50g / 지:60g
  • 최근 7일 그래프: 일별 총 칼로리 추이
```

**기술**
- 식약처 API 호출 실패 시 로컬 seed 폴백
- `diet_logs` 테이블: (profile_id, log_date, food_name, quantity)
- `food_nutrition_cache`: API 응답 캐싱

### 2. 운동 기록

```
더보기 > 마이다이어리 > 운동:
  
달리기/걷기:
  • 속도(km/h) + 시간 입력
  • ACSM 공식으로 실시간 MET 계산
  • 거리 자동 계산
  • 예: 시속 10km로 30분 → 5km, 소모 칼로리 계산

줄넘기:
  • 횟수 입력 (분당 100회 가정)
  • 소모 칼로리 계산

소모 칼로리 = MET × 체중(kg) × 시간
```

### 3. 주간 AI 리포트 (자동 생성)

```
매주 일요일 09:00에 AI Worker가 자동 생성:

┌─────────────────────────────────┐
│ [이번 주] 건강 분석             │
├─────────────────────────────────┤
│                                 │
│ 🍽️ 식단                        │
│   평균 칼로리: 2,100kcal        │
│   (목표보다 100kcal 적음)       │
│                                 │
│ 🏃 운동                        │
│   총 활동시간: 4시간 30분       │
│   소모: 2,200kcal              │
│                                 │
│ 💊 복약                        │
│   순응도: 95%                   │
│                                 │
│ 🎯 습관                        │
│   달성: 20/25개                │
│                                 │
│ 💤 수면                        │
│   평균: 7시간 30분              │
│   질: 4/5 (좋음)               │
│                                 │
│ 📝 AI 요약:                    │
│   이번 주는 운동량이 좋았고,   │
│   ...                          │
└─────────────────────────────────┘
```

**캐싱 전략**
- `weekly_reports` 테이블에 저장 (중복 생성 방지)
- AI 실패 시 규칙 기반 폴백 템플릿 사용
- 발송 대상: 복약순응도 리포트와 동일

### 4. 오늘의 한 줄 (일기)

```
더보기 > 마이다이어리 > 일기:
  • 텍스트 1줄 + 사진 1장 선택
  • 저장 시 사진 자동 압축 후 base64 저장
  • 지난 기록은 "모아보기"에서 월별/연별 보관
```

**마이그레이션**: 0038~0043 (6개)

---

## 슬라이드 14 — B② 개인화: 목표 관리 (F-GOAL 1/2/3)

**목표**: 사용자의 건강 목표를 수치화 → AI 기반 가이드 제공 → 진행률 추적

### 1. 목표 설정 (F-GOAL-1)

```
더보기 > 마이다이어리 > 목표 설정:
  
목표 입력:
  • 제목: "체중 60kg 달성"
  • 기간: 단기(≤31일) / 장기
  • 목표값: 60
  • 현재값: 75
  • 관심 질병: 당뇨, 고혈압 (기존 picker 재사용)
  
진행률 계산:
  • (75-60) / (75-60) × 100% = 0% (아직 안 함)
  • (75-65) / (75-60) × 100% = 67% (10kg 감량)
```

**CRUD**: 생성/수정/삭제 전체 구현

### 2. AI 맞춤 가이드 (F-GOAL-2)

```
목표 생성/수정 시 AI Worker 호출:

Input:
  • 목표: "체중 60kg 달성"
  • 진단질병: 당뇨, 고혈압
  • 기간: 3개월

AI Output (DietExerciseGuide):
  {
    "diet_guide": "당뇨 식이요법 기반, 저염/저탄 식단...",
    "exercise_guide": "고혈압 관리를 위해 유산소 운동...",
    "key_points": [
      "매일 아침 30분 산책",
      "야채 중심의 저염 식사",
      "스트레스 관리..."
    ]
  }

→ goal_ai_guides 테이블에 저장 (캐싱)
→ 수정할 때마다 재생성 (새로 생성하지 않으면 기존 가이드 표시)
```

**실패 시**: 규칙 기반 폴백 템플릿 사용

### 3. 일일 진행률 기록 (F-GOAL-3)

```
목표 카드:
┌─────────────────────────────────┐
│ 체중 감량: 75kg → 60kg          │
│ [████░░░░░░░░░░] 67%           │
│                                 │
│ [오늘 기록하기]                 │
└─────────────────────────────────┘

클릭 → 팝업:
  "오늘 현재 체중: ___ kg"
  → 입력 → 저장
  
→ goal_progress_logs에 기록
→ 진행률 즉시 재계산 (AI 재생성 안 함)
→ 최근 7일 추이 막대그래프 표시
```

### 4. 달성 리포트 (F-GOAL-3)

```
매주 월요일 9시 + 매월 마지막 날:
  • 주간/월간 리포트에 목표 진행률 추가
  • 예: "목표 달성: 68% (10kg 감량 진행 중)"
```

**마이그레이션**: 0045, 0046 (2개)
**테스트**: 38개 신규 테스트 전부 통과

---

## 슬라이드 15 — B③ 개인화: 수면 기록 & 홈 위젯

**문제**: REQ-TRCK-003 미구현 — 홈에서 식사/운동/수면 진행률을 한눈에 볼 수 있는 위젯이 없었음

**해결**: 수면 기록 신설 + 홈 대시보드 위젯 추가 (PR #316)

### 1. 수면 기록

```
더보기 > 마이다이어리 > 수면:
  • 수면 시간 입력 (예: 7.5시간)
  • 취침 시각 입력 (참고용)
  • 수면의 질 5단계 선택:
    ⭐⭐⭐⭐⭐ (매우 잘 잤음)
    ⭐⭐⭐⭐ (좋은 편)
    ⭐⭐⭐ (보통)
    ⭐⭐ (나쁜 편)
    ⭐ (거의 잠을 못 잤음)
  
  ※ 질이 2점 이하면 추가:
    "어제는 왜 못 잤나요?"
    (텍스트 입력)

하루 1건만 (다시 저장하면 덮어씀)
```

**구현**
- sleep_logs 테이블: (profile_id, log_date, hours, quality, reason)
- (profile_id, log_date) 유니크 제약 + upsert

### 2. 홈 대시보드 위젯

```
홈 화면 (마이다이어리 밑):
┌────────────────────────────────┐
│ 오늘의 건강 지표               │
├────────────────────────────────┤
│ 🍽️ [식사 2,100/2,300]         │ ← 클릭하면 식단 기록 화면
│    [████████░░░░░] 91%        │
│                                │
│ 🏃 [운동 400/600분]           │ ← 클릭하면 운동 기록 화면
│    [███████░░░░░░] 67%        │
│                                │
│ 💤 [수면 7h/8h]               │ ← 클릭하면 수면 기록 화면
│    [███████░░░░░░] 88%        │
└────────────────────────────────┘
```

**특징**
- 각 칸이 **독립된 버튼** (전체 카드 아님)
- 운동은 기본값 30분/일 (WHO 권장) → `EXERCISE_REFERENCE_MINUTES` 추가
- 수면은 8시간 고정 비교
- 마이다이어리 닫을 때 세 지표 리로드 → 즉시 반영

**마이그레이션**: 0053 (1개)

---

## 슬라이드 16 — B④ 개인화: 식단 칼로리 개인화

**문제 → 해결: 식단 기록할 때마다 일반적인 2,000kcal만 썼음 → 키/몸무게 기반 개인화**

**구현**: PR #328 (식단 개인화 + 습관 우선순위 + 주간리포트 BMI)

### 1. 개인화 칼로리 계산 (Mifflin-St Jeor 공식)

```
기준 칼로리 = Mifflin-St Jeor × 활동계수 × 반올림

BMR = 10×W(kg) + 6.25×H(cm) - 5×AGE + GENDER_CONST
      남성: +5  /  여성: -161

REFERENCE_KCAL = round(BMR × 1.375 / 50) × 50
                (1.375: 일반인 활동계수, 50kcal 단위)

예시:
  남성, 175cm, 70kg, 30세:
  BMR = 10×70 + 6.25×175 - 5×30 + 5 = 1,831
  기준칼로리 = round(1,831 × 1.375 / 50) × 50 = 2,950kcal

  여성, 160cm, 55kg, 30세:
  BMR = 10×55 + 6.25×160 - 5×30 - 161 = 1,254
  기준칼로리 = round(1,254 × 1.375 / 50) × 50 = 1,950kcal
```

### 2. AI 기반 한 줄 이유 생성 + 캐싱

```python
def _get_kcal_reason(profile_id, log_date, reference_kcal, personalized):
    # 1) DB 조회 → 캐시 있으면 반환
    if cached := repo.get_kcal_reason(profile_id, log_date):
        return cached["reason"]

    # 2) AI Worker 호출
    ai_reason = ai_worker.call(profile=profile, reference_kcal=reference_kcal, schema=DietKcalReasonSummary)

    # 3) (profile_id, log_date) 유니크로 저장 → 중복 방지
    repo.save_kcal_reason(profile_id, log_date, reference_kcal, ai_reason)
    return ai_reason
```

**AI Worker 출력 예시**
- 개인화됨: `"키 170cm, 몸무게 65kg 기준으로 계산된 목표입니다."`
- 일반적: `"당신의 활동 수준을 고려하면 일일 약 2,300kcal 정도가 적당합니다."`

**폴백 텍스트** (AI 실패 또는 데이터 미입력)
- 개인화됨: `"키 170cm, 몸무게 65kg을 기준으로 합니다."`
- 폴백: `"일반 성인 기준 일일 권장 섭취량입니다."`

**캐싱 효과**
- 일일 1회 AI 호출 → 비용 1/5 이상 절감
- 동일 날 재조회 시 AI 호출 안 함

### 3. 식단 기록 UI 게이팅

```typescript
// DietLogContent.tsx
const [missingBodyInfo, setMissingBodyInfo] = useState(false);

useEffect(() => {
  healthInfoApi.get().then(info => {
    setMissingBodyInfo(!info.height_cm || !info.weight_kg);
  });
}, []);

if (missingBodyInfo) {
  return (
    <MissingHealthInfoCard>
      📏 키/몸무게를 먼저 입력해주세요
      <button onClick={() => navigate('/health-info')}>
        건강정보 입력하기
      </button>
    </MissingHealthInfoCard>
  );
}

// 정상 식단 기록 UI 렌더링
```

### 4. 프론트 표시

```
식단 기록 화면:
┌─────────────────────────────────┐
│ 오늘 섭취: 1,850kcal / 2,950kcal│
│ [███████░░░░░░░░░░░░] 63%      │
│                                 │
│ ✨ 키 170cm, 몸무게 65kg을 기반 │
│    으로 계산된 목표입니다.      │
│                                 │
│ + 음식 추가                      │
└─────────────────────────────────┘
```

**마이그레이션**: 0057 (HealthProfile 분리), 0058 (DietKcalReason 캐싱)
**테스트**: 9개 (개인화 로직, 폴백, 게이팅)

---

## 슬라이드 17 — B⑤ 개인화: 습관 추천 시스템 완전 재설계

**문제 → 해결: 질병 등록해도 기본 습관과 뒤섞여 나오고, 같은 습관이 다른 질병 이름으로 중복 추천됨 → 질병 등록 시 기본 습관 노출을 끊고 AI 맞춤 습관으로 전면 교체**

**구현**: PR #337 (기존 PR #336을 브랜치 정책에 맞춰 재오픈 후 머지)

### 1. 설계 원칙: 질병 유무에 따른 완전 분기

```python
async def build_full_pool(session, profile) -> tuple[list[HabitDef], dict[str, Disease]]:
    """
    1. 질병 미등록: BASE_HABITS(8개 일반 라이프스타일 습관)만 반환
    2. 질병 등록: 기본 습관은 제외하고, 등록된 모든 질병의 AI 생성 습관만 반환
    3. 여러 질병: 각 질병의 AI 생성 습관을 모두 pool에 합침
    """
    if not profile.diagnosis_entries:
        return list(BASE_HABITS), {}
    # ... 질병별로 아래 3단계를 순서대로 시도
```

### 2. 질병당 3단계 폴백 체인

```python
for entry in profile.diagnosis_entries:
    habit_defs = []

    # 1단계: 세부 진단명(subtype)이 있으면 미리 시딩된 습관 사용
    if entry.disease_subtype is not None:
        habit_defs = await self._get_subtype_habits(session, entry.disease_subtype)

    # 2단계: 세부 진단명이 없으면 진단의 모든 정보
    #        (질환명·상세메모·경과·조절상태·약물치료)를 AI에 전달해 동적 생성
    if not habit_defs:
        habit_defs = await self._generate_habits_from_diagnosis_entry(entry)

    # 3단계: 위 둘 다 실패하면 질병별 고정 폴백 1개 사용
    if not habit_defs:
        habit_defs = [DISEASE_HABITS.get(entry.disease)]
```

### 3. 다중 질병 선택 + 라벨 기준 중복 제거

```python
def _pick_with_multiple_diseases(habits_by_disease, profile_id, today):
    # 1단계: 질병마다 최소 1개씩 로테이션으로 선택
    result = []
    for disease_habits in habits_by_disease.values():
        result.extend(_rotate(disease_habits, profile_id, today, 1))

    # 2단계: 라벨(습관명) 기준 중복 제거
    #        - 다른 질병이라도 "산책 20분"처럼 같은 이름이면 하나만 유지
    seen_labels = set()
    deduped = [h for h in result if h.label not in seen_labels and not seen_labels.add(h.label)]

    # 3단계: 5개 미만이면 남은 질병 습관 중 안 겹치는 것으로 채움
    return deduped[:MAX_RECOMMENDATIONS]
```

### 4. 습관별 추천 이유 세분화 생성

```python
def _generate_detailed_reason(self, disease: Disease, habit_label: str) -> str:
    """질병 6종(당뇨/심장/뇌혈관/간/암/기타) × 습관 키워드별로
    구체적인 추천 이유를 생성 — 왜 이 습관이 이 질병에 도움이 되는지 설명"""
    handlers = {
        Disease.DIABETES: self._get_diabetes_reason,
        Disease.LIVER_DISEASE: self._get_liver_disease_reason,
        Disease.HEART_DISEASE: self._get_heart_disease_reason,
        Disease.CEREBROVASCULAR_DISEASE: self._get_cerebrovascular_reason,
        Disease.CANCER: self._get_cancer_reason,
        Disease.OTHER: self._get_other_disease_reason,  # 골절/염좌/장염 등
    }
    return handlers[disease](habit_label)


# 예: 당뇨 + "산책 20분" → "당뇨병 혈당 조절에 효과적인 유산소 운동입니다"
#     간질환 + "산책 20분" → "간질환 관리에 효과적인 가벼운 운동입니다"
#     같은 습관이어도 질병에 따라 다른 이유 문구 생성
```

### 5. AI 생성 대기 중 로딩 화면

```tsx
// HabitRecommendationModal.tsx — loading 중에는 스피너 전용 화면 표시
{loading && (
  <>
    <Spinner />  {/* 50px 원형 스피너, 1초 회전 애니메이션 */}
    <p>습관을 분석 중입니다...</p>
    <p>당신의 건강 상태에 맞춘 습관을 추천하고 있습니다</p>
  </>
)}
```

**효과**
- 질병 등록 시 기본 습관이 섞여 나오던 문제 완전 제거
- 같은 이름의 습관이 여러 질병으로 중복 추천되던 문제 해결 (라벨 기준 중복 제거)
- 습관마다 "왜 이 습관이 내 질병에 필요한지" 구체적 이유 확인 가능
- AI 응답을 기다리는 동안 빈 화면 대신 진행 상태 표시로 체감 대기시간 ↓

**테스트/품질**
- Ruff 린터·포매터·Mypy 타입체크 전부 통과 (복잡도 높던 함수 2개를 다수의 헬퍼 함수로 분리)
- 습관 정의를 `TypedDict`로 명시해 AI 응답 필드 타입 안전성 확보

---

## 슬라이드 18 — B⑥ 개인화: 주간 리포트 BMI + 목표 + 수면 통합

**목표**: 주간 리포트에 신체 정보(BMI) + 목표 진도 + 수면 데이터까지 통합

**구현**: PR #328 + #301 (리포트 요일 통일)

### 1. 주간 리포트 최종 구조

```
┌─────────────────────────────────┐
│ 🌟 [홍길동] 님의 이번 주 생활  │
├─────────────────────────────────┤
│ 📊 신체 정보:                  │
│    키 170.00cm, 몸무게 65.00kg │
│    BMI 22.5 (정상 범위)        │
│                                 │
│ 🎯 목표 진도:                  │
│    체중 감량: 75kg → 60kg      │
│    진행률: 67% (10kg 감량)    │
│                                 │
│ 📈 통계:                       │
│    • 평균 칼로리: 2,100kcal    │
│    • 운동 활동: 400분 (67%)    │
│    • 평균 수면: 7h 30m        │
│    • 수면의 질: 4/5 (좋음)     │
│    • 복약순응도: 95%           │
│    • 습관달성: 20/25           │
│                                 │
│ 💭 AI 종합 분석:               │
│    이번 주는 운동량이 좋았고, │
│    식단 조절도 잘했습니다.    │
│    다음 주도 화이팅!           │
└─────────────────────────────────┘
```

### 2. BMI 계산 및 표시

```python
def _body_info_line(profile) -> str:
    health = profile.health_profile
    if not (health and health.height_cm and health.weight_kg):
        return ""

    height = health.height_cm
    weight = health.weight_kg
    bmi = weight / (height / 100) ** 2

    return f"키 {height:.2f}cm, 몸무게 {weight:.2f}kg (BMI {bmi:.1f})"


# 테스트
# height=170, weight=65:
# bmi = 65 / (1.7**2) = 22.5 ✓
```

### 3. 마이그레이션 및 테스트

**마이그레이션**: 없음 (기존 테이블 구조 재사용)

**테스트**: 5개
- BMI 계산 정확성
- 신체 정보 미입력 시 표시 안 함
- 목표 진도 계산
- 주간 리포트 생성 시 모든 데이터 포함
- 발송 요일 일요일 통일 (PR #301)

---

## 슬라이드 19 — B⑦ 개인화: AI 피드백 루프 (👍/👎)

**문제 → 해결: AI/규칙 기반으로 생성되는 습관 추천 이유, 식단 칼로리 이유가 실제로 도움이 되는지 확인할 방법이 전혀 없었음 → 평가 채널 신설**

**구현**: PR #345

### 1. 설계: 대상마다 고유 PK가 없는 문제

```python
class ReasonFeedback(Base):
    """습관 추천 이유(요청마다 새로 계산, DB row 없음)와 식단 칼로리 이유
    ((profile_id, log_date)로만 식별)는 chat_message_feedbacks처럼 단일 FK로
    특정할 수 없다 - (feature, target_key) 조합으로 느슨하게 대상을 식별한다."""

    profile_id: int
    feature: ReasonFeedbackFeature  # HABIT_REASON | DIET_KCAL_REASON
    target_key: str  # habit_key 또는 log_date(ISO)
    value: FeedbackValue  # UP | DOWN
    comment: str | None

    # (profile_id, feature, target_key) 유니크 - 재평가 시 upsert
```

### 2. 엔드포인트 2개 (기존 로직 재검증으로 무결성 보장)

```http
POST /habits/{habit_key}/reason-feedback
  → habit_key가 "오늘의 추천 목록"에 없으면 404 (엉뚱한 값 평가 방지)

POST /diet/kcal-reason-feedback
  → 오늘의 칼로리 이유가 아직 생성 안 됐으면 404
```

### 3. 프론트: 공용 컴포넌트 하나로 두 기능 재사용

```tsx
<ReasonFeedback onSubmit={(value) => habitApi.submitReasonFeedback(habit.key, value)} />
<ReasonFeedback onSubmit={(value) => dietApi.submitKcalReasonFeedback(value)} />
```

**효과**
- 습관/식단 이유 문구 아래 👍/👎 + "감사합니다!" 확인 문구
- 재평가 가능(upsert), 실패해도 화면 흐름을 막지 않음(조용히 무시)

**테스트**: 신규 6개(habit 3개 + diet 3개) 전부 통과
**마이그레이션**: 0062 (reason_feedbacks 테이블 신설)

---

## 슬라이드 20 — B⑧ 개인화: 식단 검색 UX 개선

**문제 → 해결: 검색 결과가 여러 개면 카드마다 인분선택+기록버튼이 다 붙어 한눈에 보기 힘들다는 실사용 피드백**

**구현**: PR #346

### Before → After

```
Before: 검색 결과 N개 각각에
  [흰쌀밥]   [0.5/1/1.5/2인분] [기록하기]
  [현미밥]   [0.5/1/1.5/2인분] [기록하기]
  [잡곡밥]   [0.5/1/1.5/2인분] [기록하기]
  ...        → 결과가 많을수록 스크롤과 클릭 대상이 폭증

After: 결과를 간결한 목록으로 나열 → 하나 선택 → 그 아래에서만 설정
  국밥_돼지머리   1인분 100g · 137kcal
  국밥_순대국밥   1인분 100g · 75kcal   ← 선택
  기장밥         1인분 100g · 166kcal
  ┌─────────────────────────────────┐
  │ 국밥_순대국밥  100g · 75kcal     │
  │ [0.5][1인분][1.5][2인분][기타]  │
  │        [기록하기]                │
  └─────────────────────────────────┘
```

### 구현 포인트

```tsx
// 이전: multipliers/customMode/customText를 food_name으로 keying(Record<string, T>)
// 이후: 선택된 하나만 다루므로 단일 state로 단순화
const [selectedIndex, setSelectedIndex] = useState<number | null>(null);
const [multiplier, setMultiplier] = useState(1);
```

**효과**: 결과 개수와 무관하게 클릭 대상 고정(목록 클릭 1회 + 설정), 화면 스크롤량 감소

---

## 슬라이드 21 — B⑨ 개인화: 운동 기록 UX 개선 + AI 인식

**문제 → 해결: 웨이트/근력운동처럼 세부 종목별 입력까지 지원할 계획이 없는 고정된 23개 운동 목록인데 자유 검색 UI를 쓰고 있었음 → 드롭다운으로 전환, 목록에 없는 운동은 AI가 인식**

**구현**: PR #346

### 1. 드롭다운 전환

```
Before: [운동 이름 입력_______] [검색]  → 검색 결과 카드 여러 개
After:  [운동 종류를 선택하세요 ▾]      → 고정 23개 + "기타(직접 입력)"
```

```python
@exercise_router.get("/catalog")  # 신규: 검색 없이 전체 23개 그대로 반환
async def get_exercise_catalog(profile): ...
```

### 2. "기타(직접 입력)" → AI로 MET 값 인식

```
운동 종류 선택: 기타(직접 입력)
  ↓
[운동 이름 입력_______________]
  ↓ (클릭)
[✨ AI로 인식하기]
  ↓
✨ AI 인식 결과: MET 4.0 · 예상 소모 120kcal
[10분][20분][30분][60분]  [기록하기]
```

```python
async def estimate_met(self, exercise_name: str) -> ExerciseMetEstimateResult:
    """AI가 Compendium of Physical Activities 기준 MET을 추정한다.
    실패하거나 비정상적인 값(환각, 0.5~25 범위 밖)이면 항상 폴백(4.0)으로
    대체해 절대 막히지 않는다."""
    try:
        result = await self._gateway.call_structured(...)
        met = result.met_value
        if not (0.5 <= met <= 25.0):
            met = _FALLBACK_MET
    except (AIWorkerUnavailableError, ...):
        met = _FALLBACK_MET
    return ExerciseMetEstimateResult(exercise_name=exercise_name, met_value=met)
```

추정된 MET은 기존 "duration 모드" 기록 로직을 그대로 재사용(백엔드 기록 로직 추가 변경 없음).

**효과**
- 고정 목록은 드롭다운으로 단순화, 목록에 없는 운동도 AI로 즉시 커버
- AI 응답 실패해도 100% 가용성 보장 (폴백 설계 원칙 재사용)

**테스트**: 신규 6개(서비스 4개 + API 2개) 전부 통과
**엔드포인트 신규**: `GET /exercise/catalog`, `POST /exercise/estimate-met`

---

## 슬라이드 22 — B⑩ UI 통일화

**문제 → 해결: 전체 화면의 아이콘/버튼 스타일이 제각각 → 표준화 (PR #305)**

### 1. 제목 아이콘 표준화 (14개 화면)

```
기존: 이모지(📅💊👨‍👩‍👧) 제각각
개선: lucide 라인아트 아이콘 통일

| 화면 | 아이콘 | 화면 | 아이콘 |
|---|---|---|---|
| 복약스케줄 | Calendar | 개인건강정보 | HeartPulse |
| 복약관리 | Pill | 복약시간표 | Clock |
| 가족관리 | Users | 주간리포트 | BarChart3 |
| 응급안내 | Siren | 트래커 | Activity |
| 더보기 | LayoutGrid | 공지사항 | Megaphone |
| 알림설정 | Bell | 동의화면 | ShieldCheck |
| 생활습관정보 | Moon | 컨텐츠생성 | Sparkles |
| 개인정보수정 | Settings | 다이어리 | NotebookPen |

PageTitle 컴포넌트:
<PageTitle icon={Calendar}>복약스케줄</PageTitle>
→ 36px 원형 배경 + 아이콘 + h1 자동 적용
```

### 2. 버튼 모서리 표준화

```
기본: 10px
예외:
  • 카드형 버튼(응급안내, 더보기, 홈 바로가기): 16px
  • 토글 필모양: 999px
  • 원형 아이콘: 50%

작업 중 발견 & 함께 고침:
  • 제목 여백 소실: marginBottom 12 추가
  • 팝업 패널 어긋남: 8/12px → 10px 정렬
  • DUR 화면 팔레트: pinkTheme 토큰 통일 (다크모드 지원)
```

**테스트**: 16개 화면 DOM 실측
**마이그레이션**: 없음

---

## 슬라이드 23 — B⑪ 목표 개선: 횟수형 목표 구분

**개선**: PR #320 (T-GOAL-2)

```
기존: 모든 목표가 수치형(체중, BMI 등)
개선: NUMERIC(수치형) / FREQUENCY(횟수형) 구분

FREQUENCY 예시:
  • "일주일에 명상 4회"
  • "월 2번 영화 보기"
  • "주 5일 운동"

UI:
  수치형: [기록하기] → 숫자 입력
  횟수형: [오늘 완료] → 1회 클릭 시 +1 (당일 중복 방지)

마이그레이션: 0054 (goal_type 컬럼 추가)
테스트: 38개 신규 + 회귀 0개
```

---

## 슬라이드 24 — 기술적 하이라이트

### 알림 시스템

1. **스마트 묶음 발송**
   - 같은 시각 N개 알림 → 1회 발송 (배터리 ↓, 사용자 피로 ↓)
   - push_scheduler 로직 최적화

2. **알림함 통합 & 딥링크**
   - PushService 한 곳의 훅 → 모든 알림 자동 저장 (확장성 ↑)
   - link_url 매핑 → 클릭 즉시 관련 화면 이동

3. **스누즈 재알림**
   - 1회성 snooze_reminders → 예약 후 자동 삭제 (DB 부하 ↓)

4. **라이프스타일 팁 세밀 제어**
   - 전용 시간대 + 빈도 설정 → 사용자 피로도 관리

5. **안드로이드 호환성**
   - registration.showNotification() 사용 → 실제 푸시와 동일 패턴

### 개인화 시스템

1. **캐싱 전략**
   - AI 호출 (profile_id, log_date) 유니크 캐싱 → 일일 1회 호출 (비용 1/5)
   - 주간 리포트도 date-based 캐싱

2. **폴백 설계**
   - AI 실패 시 규칙 기반 템플릿 (100% 가용성)
   - 불완전한 데이터도 기본값으로 서비스

3. **데이터 정합성**
   - HealthProfile 별도 테이블 (PII 분리)
   - 마이그레이션 4개 (0044~0048) + (0053, 0057, 0058)

4. **UI 게이팅**
   - 식단 기록 진입 시 건강정보 체크 → 필수 입력 강제

5. **목표 진행률**
   - 매일 수치 기록 → 7일 추이 그래프
   - 주간/월간 리포트에 반영

6. **습관 추천 재설계**
   - 질병 등록 시 기본 습관 배제, AI 맞춤 습관만 노출 (3단계 폴백 체인)
   - 다중 질병 시 라벨 기준 중복 제거 + 질병당 최소 1개 보장
   - 습관 × 질병 조합별 세분화된 추천 이유 자동 생성

7. **다중 데이터 수집**
   - 식단/운동/수면/목표 + 일기까지 통합
   - 각자 독립적 기록이지만 주간 리포트에 통합

8. **AI 피드백 루프**
   - 대상 자체가 DB row로 존재하지 않는 습관 이유도 (feature, target_key) 조합으로 느슨하게 식별
   - 재평가 upsert + 실패해도 화면 흐름을 막지 않는 조용한 실패 처리

9. **AI 폴백 견고성 확장**
   - 운동 MET 추정: AI 실패는 물론 "환각"으로 비정상 값(0.5~25 범위 밖)을 줘도 폴백으로 대체
   - 기존 폴백 원칙(습관/식단)을 새 기능에도 일관되게 적용

10. **UX 복잡도 관리**
    - 식단 검색: 결과 개수와 무관하게 "목록 선택 → 설정" 2단계로 클릭 대상 고정
    - 운동 기록: 자유 검색 대신 드롭다운 + AI 인식으로 입력 방식 이원화

### 전체 시스템

- **마이그레이션**: 총 16개 (무중단 배포)
- **테스트**: 32개 신규 (회귀 0개)
- **PR**: 18개 (총 5,000+ 라인 추가/수정)
- **코드 품질**: ruff/mypy/tsc/eslint 전부 통과

---

## 슬라이드 25 — 체감한 한계 및 다음 과제

### 알림 시스템

- **액션 버튼 제약**: 브라우저 푸시 API 한계(액션 2개) → 스누즈는 인앱 바텀시트로 우회
- **웹푸시 초기화**: 부분 사용자가 미구독 → 인앱 알림함 수동 오픈 필요
- **빈도 설정 정교성**: 현재 "N일 간격" 간단한 규칙 → 요일/시간대 조합 스케줄링 확장 여지

### 개인화 시스템

- **AI 응답 품질**: AI Worker가 항상 의료적으로 정확한 한 줄을 생성하지 않음 → 응답 필터링/도메인 파인튜닝 필요
- **캐시 일관성**: 일일 캐시는 그 날 안의 건강정보 변경 감지가 정확하지만, 주간 리포트 데이터는 주 단위 집계와 불일치 가능 → 집계 시점 명확화
- **AI 습관 생성 지연**: 세부 진단명이 없으면 매번 AI 호출이 필요해 응답까지 대기시간 발생 → 로딩 화면으로 체감시간은 낮췄지만 결과 캐싱은 아직 없음
- **폴백 다양성**: 세부 진단/AI 생성 모두 실패했을 때의 최종 폴백은 질병당 1개뿐 → 폴백 옵션도 3-5개로 확장 여지
- **영양학적 정확성**: Mifflin-St Jeor 공식은 근사치 → 활동계수 개인화(1.2~1.9 범위) 확장 가능
- **피드백 데이터 미활용**: 👍/👎는 저장만 될 뿐, 아직 프롬프트 개선이나 모델 재학습에 실제로 반영되는 루프는 아님 → 다음 단계로 집계·분석 필요
- **AI MET 추정 캐싱 없음**: 같은 "기타" 운동명을 반복 입력해도 매번 AI를 다시 호출함 → 습관 subtype처럼 (exercise_name) 캐싱 여지

---

## 슬라이드 26 — 시연 영상 (시나리오)

*이 8가지를 6~8분 안에 순차적으로 보여줄 수 있습니다*

### A. 알림 시스템 시연

**1단계: 복약 알림 & 스마트 묶음**
```
(1) 약 등록: 타이레놀 500mg (09:00 매일)
(2) 약 등록: 종합비타민 (09:00 매일)
(3) 시간 경과 → 09:00 푸시 1개 수신
    "타이레놀, 종합비타민 드실 시간이에요!"
(4) 푸시의 [복용완료] 클릭 → 두 약 모두 기록
```

**2단계: 스누즈**
```
(1) 알림 본문 탭 → 바텀시트 열림
    [ 30분 후에 ]
    [ 1시간 후에 ]
(2) "30분 후에" 선택
(3) 30분 후 자동 재알림 수신
```

**3단계: 알림함 & 딥링크**
```
(1) 홈 상단 🔔 버튼 클릭
(2) 알림함 패널 열림 (최근 알림들 보기)
(3) "타이레놀 알림" 클릭 → /alarms로 즉시 이동
(4) "주간 리포트" 클릭 → /weekly-reports로 이동
```

**4단계: 라이프스타일 팁 설정**
```
(1) 설정 > 알림
(2) "라이프스타일 팁 알림" 토글 ON
(3) 시간대: 09:00~21:00 / 빈도: 3일에 한 번 선택
(4) 저장
```

### B. 개인화 시스템 시연

**1단계: 건강정보 입력**
```
(1) 더보기 > 개인건강정보
(2) 키 170cm / 몸무게 65kg / 성별 남성
    질병: 당뇨, 고혈압 입력
(3) 저장
```

**2단계: 식단 기록 개인화 + 검색 UX**
```
(1) 더보기 > 마이다이어리 > 식단
(2) 칼로리 바 표시: "2,950kcal (개인화)"
    설명: "✨ 키 170cm, 몸무게 65kg을 기준으로..."
    → 👍 클릭 → "감사합니다!" (AI 피드백 루프)
(3) "밥" 검색 → 결과 목록(흰쌀밥/현미밥/잡곡밥/...) 나열
(4) "김치찌개" 선택 → 그 아래에 인분 선택 + 기록하기 카드 1개만 등장
(5) 1인분 선택 → 저장 → 오늘 칼로리 누적 표시
```

**3단계: 운동 기록 (드롭다운 + AI 인식)**
```
(1) 더보기 > 마이다이어리 > 운동
(2) 드롭다운에서 "달리기" 선택 → 속도(km/h) + 시간 입력
    → 소모칼로리 자동 계산 & 저장
(3) 다시 드롭다운 → "기타(직접 입력)" 선택
(4) "클라이밍" 입력 → [✨ AI로 인식하기] 클릭
    → "AI 인식 결과: MET 4.0 · 예상 소모 120kcal"
(5) 30분 선택 → 저장 → 기록 목록에 "클라이밍" 추가
```

**4단계: 수면 기록**
```
(1) 더보기 > 마이다이어리 > 수면
(2) 수면 시간: 7.5h / 수면의 질: ⭐⭐⭐⭐ 선택
(3) 저장
(4) 홈 위젯 확인: 식사/운동/수면 진행률 바 3칸
    → 각 칸 클릭하면 해당 기록 화면으로 이동
```

**5단계: 목표 설정**
```
(1) 더보기 > 마이다이어리 > 목표 설정
(2) "체중 60kg 달성" 목표 생성
    → AI가 자동으로 가이드 생성 (식단/운동)
(3) "오늘 기록하기" → 현재 체중 입력
(4) 진행률 업데이트: 70% → 75% (즉시 반영)
```

**6단계: 습관 추천 (질병 특화 AI 생성) + 피드백**
```
(1) 홈 > "추천 라이프 스타일" 카드 클릭
(2) "습관을 분석 중입니다..." 로딩 스피너 → AI 생성 습관만 노출
    "식후 10분 걷기 - 💡 식후 혈당 상승 억제에 가장 효과적인 운동입니다"
(3) "이 설명이 도움이 됐나요?" 👍 클릭 → "감사합니다!"
(4) 습관 선택 > 저장
```

**7단계: 일기 (오늘의 한 줄)**
```
(1) 더보기 > 마이다이어리 > 일기
(2) "오늘 하루 좋았다~" + 사진 1장 선택
(3) 저장 → 모아보기에 추가
```

**8단계: 주간 리포트**
```
(1) 더보기 > 주간 리포트
(2) 최신 리포트 열기
    • 신체정보: "키 170cm, 몸무게 65kg, BMI 22.5"
    • 식단/운동/수면 통계
    • 목표 진도: "체중 감량 75% 진행"
    • AI 종합 분석
(3) 이전 리포트도 월별 보관함에서 조회
```

---

## 슬라이드 27 — 참고: 발표에서 "있어보이게" 만드는 포인트

### 1. 숫자로 말하기

**알림 시스템**
- "같은 시각 약 N개 → 알림 1회로 통합"
- "브라우저 액션 제약(2개)을 스누즈 바텀시트로 **우회**"
- "6개 PR(280/282/298/299/302/323), **0개 회귀**"

**개인화 시스템**
- "마이그레이션 5개 추가 (알림함 + 칼로리 캐싱 + 건강정보 분리 + 피드백 테이블)"
- "테스트 32개 신규 (회귀 0개)"
- "AI 캐싱으로 Worker 호출 **1회/일 → 평균 1/5 비용 절감**"
- "AI 실패해도 **100% 응답 보장** - 습관/식단/운동 MET 셋 다 폴백 설계"
- "총 18개 PR, 5,000+ 라인 변경"

### 2. 문제 → 해결 서사

**알림**
- "복약 알림 받으면 지금 못 먹는데 미룰 방법이 없었음 → **스누즈 바텀시트 추가**"
- "팁 알림을 일반 알림과 같은 시간대로만 제어 → **전용 시간대 + 빈도 설정 추가**"
- "알림 받아도 어디로 가야 할지 몰랐음 → **딥링크로 즉시 이동**"

**개인화**
- "식단 기록할 때마다 건강정보 다시 물었음 → **게이팅으로 필수 입력 강제**"
- "질병 등록했는데도 기본 습관과 섞여 나오고 같은 습관이 중복 추천됨 → **기본 습관 배제 + 3단계 AI 폴백 + 라벨 중복 제거로 완전 재설계**"
- "홈에서 일일 지표를 볼 수 없었음 → **식사/운동/수면 진행률 위젯 신설**"
- "AI가 생성하는 추천 이유가 실제로 도움이 되는지 확인할 방법이 없었음 → **👍/👎 피드백 루프 신설**"
- "식단 검색 결과마다 인분선택+기록버튼이 다 붙어 여러 개면 보기 힘들었음 → **목록 선택 → 설정 흐름으로 재설계**"
- "운동은 고정 23개뿐인데 자유 검색 UI를 쓰고 있었음 → **드롭다운 전환 + '기타'는 AI가 MET 값 인식**"

### 3. 아키텍처의 우아함

- "PushService **한 곳의 훅** → 모든 알림 종류 자동 커버" (확장성 좋음)
- "HealthProfile 별도 테이블 → **PII 분리 + 다른 기능과 독립**"
- "(profile_id, log_date) 유니크 제약 → **중복 저장 자동 방지 + 캐싱 정책 명확**"
- "AI 폴백 템플릿 → **100% 가용성 보장** (AI 실패 시에도 서비스 중단 없음)"
- "(feature, target_key) 조합 식별 → **DB row가 없는 대상(습관 이유)까지 하나의 피드백 테이블로 커버**"

### 4. 기술 깊이

**Mifflin-St Jeor 공식 적용**
- 단순 "2000kcal 일괄 적용"이 아니라
- "키/몸무게/성별/나이 모두 고려 + 활동계수 1.375 + 50kcal 단위 반올림"
- "개인별로 1,500~3,200kcal 범위의 개인화"

**로테이션 결정론적 선택**
- "(today.toordinal() + profile_id) % len(pool)"
- 같은 사용자는 매일 같은 습관, 사용자마다 다른 습관
- 예측 가능성 ↑

**AI 환각 방어**
- 단순 "AI가 준 값을 그대로 쓰지 않는다"
- "MET 추정값이 0.5~25 범위를 벗어나면(환각) 자동으로 폴백값 대체"
- "AI 실패와 AI의 이상 응답, 두 가지 실패 모드를 모두 방어"

---

## 부록: 통계 요약

| 항목 | 수치 |
|---|---|
| **PR 개수** | 18개 |
| **마이그레이션** | 16개 (무중단 배포) |
| **코드 라인** | 5,000+ 추가/수정 |
| **테스트** | 32+ 신규 (회귀 0개) |
| **품질 체크** | ruff/mypy/tsc/eslint 전부 통과 |
| **신규 API 엔드포인트** | 4개 (습관 이유 피드백, 식단 이유 피드백, 운동 카탈로그, 운동 AI MET 추정) |
| **기능** | 알림 6개 + 개인화 12개 + UI 1개 + 유지보수 1개 |

---

## 발표 자료 사용 가이드

1. **슬라이드 6-7**: 팀 역할 + 수행 절차 (전체 구조)
2. **슬라이드 8**: 수행 경과 개요 (항목 정렬)
3. **슬라이드 9-23**: 각 기능 상세 (선택적 축약 가능, 19-21이 이번에 새로 추가된 슬라이드)
4. **슬라이드 24**: 기술 하이라이트 (강점 강조)
5. **슬라이드 25**: 한계 (겸손하고 구체적)
6. **슬라이드 26**: 시연 (6~8분 라이브 시연)
7. **슬라이드 27**: 발표 팁 (포인트 정리)

발표 시간에 맞춰 슬라이드 9-23 중 핵심만 선택해서 진행하면 됩니다. 상세 내용은 구글 프레젠테이션 발표자 노트에 넣으면 좋습니다.
