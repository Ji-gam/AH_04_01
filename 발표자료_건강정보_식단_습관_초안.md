# 발표자료 초안 — 건강정보 / 식단 기록 / 습관 추천 시스템 파트

> 전달받은 PPT 템플릿 목차에 맞춰 **02(팀 구성 및 역할) / 03(수행 절차 및 방법) / 04(수행 경과)** 슬라이드 내용을 작성했습니다.
> 모든 수치와 사실관계는 코드베이스, 테스트, git log를 근거로 했습니다.

---

## 슬라이드 6 — 02. 프로젝트 팀 구성 및 역할

**담당: 알림 시스템 + 건강정보 기반 식단 / 습관 개인화**

담당 업무:

**1. 알림 시스템 (FCM 기반)**
- 복약 알림 액션 개선: 복용완료/빈도줄이기 2가지 인앱 액션 버튼 (F-NTFY-1)
- 스마트 알림 묶음 발송: 같은 시각 겹치는 알림 통합 (F-NTFY-2)
- 알림 미루기(스누즈): 바텀시트 UI로 "30분/1시간 후"에 다시 알려주기 (F-NTFY-3)
- 라이프스타일 팁 커스텀 설정: 전용 시간대 + 수신 빈도 설정 (F-NTFY-6)
- 알림함 구현: 모든 알림을 한 곳에서 확인 + 관련 페이지로 즉시 이동
- 안드로이드 호환성 수정: 푸시 수신/테스트 버튼 안정화

**2. 건강정보 기반 식단 / 습관 개인화**
- 사용자 건강정보(키/몸무게/질병) 수집 및 건강 프로필 관리
- 식단 기록 시 개인화된 칼로리 목표 계산 (Mifflin-St Jeor 공식 기반)
- AI를 활용한 칼로리 목표 설정 이유 자동 생성 및 캐싱
- 습관 추천 시스템 개선 — 질병 관련 습관 우선순위 및 AI 기반 동적 추천
- 주간 리포트 생성 시 BMI 포함 (신체 정보 통합 분석)
- 식단 기록 UI 개선 — 필수 정보 미입력 시 건강정보 페이지로 자동 유도
- 홈 화면 습관 선택 모달 라우팅 최적화

*(다른 팀 부분: 위치정보 응급 안내, 목표 관리, 수면 기록 등 전체 UI/UX 디자인)*

---

## 슬라이드 7 — 03. 프로젝트 수행 절차 및 방법

**"기능 추가 → 문제 해결 → 사용성 개선"의 반복 순환**

| 구분 | 활동 | 비고 |
|---|---|---|
| **알림 시스템 구축** | | |
| 1단계: 기본 액션 | 복용완료/빈도줄이기 버튼 추가, 같은 시각 알림 묶음 발송 | PR #280: F-NTFY-1, F-NTFY-2 |
| 2단계: 사용자 경험 | 스누즈(미루기) 바텀시트 추가, 라이프스타일 팁 커스텀 설정 | PR #323 (스누즈), PR #282 (수신 빈도) |
| 3단계: 발견성 | 홈 상단 🔔 알림함 추가, 알림 클릭 → 관련 페이지 이동 | PR #299, #302 (알림함 + 딥링크) |
| 4단계: 안정화 | 안드로이드 호환성 버그 수정 | PR #298 (클라이언트 버그) |
| | | |
| **개인화 시스템 구축** | | |
| 데이터 설계 | Profile ↔ HealthProfile 1:1 관계, DietKcalReason 캐싱 | 마이그레이션 0057, 0058 |
| 백엔드 로직 | Mifflin-St Jeor 공식, AI 기반 칼로리 목표 이유 | 불완전 데이터도 폴백 처리 |
| 습관 추천 재설계 | 질병 우선순위 플래그, 풀링 시 분류 | 기존 로테이션 유지 + 우선순위 |
| 프론트엔드 UI | 건강정보 게이팅, 칼로리 이유 표시, 홈 모달 라우팅 | 세 화면 동시 수정 |
| 테스트 및 검증 | 단위 테스트 16개 + E2E | 모든 엣지 케이스 커버 |
| 배포 | dev 브랜치 PR #328 머지, CI 통과 | 라이브 환경 검증 |

---

## 슬라이드 8 — 04. 프로젝트 수행 경과 (개요)

이어지는 슬라이드는 다음 순서로 구성:

**A. 알림 시스템 파트**
1. **스마트 알림 발송** — 복용완료 액션 + 시간대 묶음
2. **알림 미루기 & 커스터마이징** — 스누즈 바텀시트, 수신 빈도 설정
3. **알림함 통합** — 모든 알림 한 곳에서 관리, 딥링크 이동
4. **안정성 개선** — 안드로이드 호환성 수정

**B. 개인화 시스템 파트**
1. **데이터 아키텍처** — 건강정보 분리 설계 (Profile ↔ HealthProfile)
2. **기능 설계 ① 식단 개인화** — Mifflin-St Jeor 공식, AI 기반 한 줄 이유 생성
3. **기능 설계 ② 습관 추천** — 질병 우선순위, AI 동적 추천
4. **기능 설계 ③ 주간 리포트** — BMI 포함, 신체 정보 통합
5. **시연** — 실사용 시나리오

---

## 슬라이드 9 — A① 알림 시스템: 스마트 발송 (복용완료 + 시간대 묶음)

**문제 상황**
- 기존: 알림 액션이 "30분 후"만 가능했고, 같은 약 여러 개도 따로따로 발송
- 사용자: 약 때마다 "복용했다" 표시를 못 함, 같은 시각 약들이 계속 울림

**해결: F-NTFY-1, F-NTFY-2**

### 1. 복용완료 액션 추가 (F-NTFY-1)

```
푸시 알림에 액션 버튼 2개 표시:
[ 복용완료 ]  [ 빈도 줄이기 ]

→ "복용완료" 클릭 시 medication_intake_logs에 즉시 기록
→ 누른 약은 약력에 자동 반영 (복약순응도 ↑)
```

### 2. 같은 시각 알림 묶음 발송 (F-NTFY-2)

```python
# 예: 09:00에 약 2개 (타이레놀, 종합비타민)
# 기존: 두 번 울림
#   09:00:01 - "타이레놀 드실 시간이에요!" 
#   09:00:02 - "종합비타민 드실 시간이에요!"

# 개선: 한 번 울림
#   09:00 - "타이레놀정, 종합비타민 드실 시간이에요!"
#
# 액션 버튼은 전체에 적용 (둘 다 복용, 또는 둘 다 빈도 줄이기)
```

**구현 포인트**
- 같은 프로필의 같은 **분(minute)**에 겹치는 알림만 묶음
- 직접등록 알림 + 트래커 약도 함께 묶임
- 액션 버튼의 요청은 서비스워커가 각 항목마다 반복 실행

**DB 마이그레이션**
- 마이그레이션 0047: 알림 스케줄링 로직 최적화, 기존 snooze 코드 제거

**테스트**
- 같은 시각 약 2개 등록 → 한 묶음으로 발송 확인
- "복용완료" 클릭 → medication_intake_logs에 기록되고, 중복 클릭 시 안 중복 저장

---

## 슬라이드 10 — A② 알림 시스템: 스누즈 & 커스터마이징

**문제 상황**
- 기존: 알림이 오면 지금 당장 약을 못 먹는 경우, 미루는 방법이 없음
- 사용자: "좀 있다 다시 알려줄 수는 없을까?"

**해결: F-NTFY-3 (스누즈), F-NTFY-6 (라이프스타일 팁 커스터마이징)**

### 1. 스누즈: 알림 미루기 (F-NTFY-3)

```
복약 알림을 받으면:
  1) 알림 제목 부분(액션 버튼 아닌 부분)을 탭
  2) 인앱 바텀시트 열림:
     [ 30분 후에 ]
     [ 1시간 후에 ]
  3) 선택 → 서버가 snooze_reminders 테이블에 저장
  4) 예정된 시각에 알림 재발송 후 자동 삭제
```

**데이터 구조**
```python
class SnoozeReminder(Base):
    id: int
    medication_id: int
    original_alarm_id: int
    snooze_until: datetime  # "30분 후" = now + 30min
    created_at: datetime
```

**동작**
- push_scheduler 매분 틱마다 `snooze_until` 시각이 지난 항목 재발송 → 삭제
- 여러 번 스누즈 가능 (사용자 자율)

### 2. 라이프스타일 팁 커스터마이징 (F-NTFY-6)

```
기존 무음 시간대(quiet_start/end)와 별개로,
"라이프스타일 팁" 알림만 더 세세하게 설정 가능:

┌─────────────────────────────────┐
│ 라이프스타일 팁 알림            │ (토글)
├─────────────────────────────────┤
│ ⚙️ 수신 시간대                  │
│    09:00 ~ 21:00               │ (기본값, 끄면 기존 무음시간 사용)
│                                 │
│ ⚙️ 수신 빈도                    │
│    ○ 제한 없음                  │
│    ○ 하루에 한 번               │
│    ○ 3일에 한 번   ← (선택)    │
│    ○ 일주일에 한 번             │
└─────────────────────────────────┘

→ 서버가 lifestyle_tip_last_sent_at 추적,
  마지막 발송 이후 설정된 간격이 경과했는지 확인 후 발송
```

**구현**
- 알림설정 화면에 "라이프스타일 팁" 토글 추가
- 토글이 꺼져있으면 하위 옵션(시간대/빈도) 숨김
- `content_notification_service.py`에서 발송 전 2가지 조건 검사:
  1) 무음 시간대 밖인가?
  2) 수신 빈도 간격 경과했는가?

**테스트**
- 시간대 안/밖 / 간격 경과/미경과 4가지 조합 → 올바른 판정 확인

---

## 슬라이드 11 — A③ 알림 시스템: 알림함 통합 & 딥링크

**문제 상황**
- 기존: 알림을 받으면 app에 들어와야 하는데, 어느 화면에 가야 할지 명확하지 않음
- 사용자: "복약 알림 받았는데 약 이력은 어디에 있더라?"

**해결: 알림함 통합 (PR #299) + 딥링크 (PR #302)**

### 1. 홈 상단 🔔 알림함

```
홈 화면 상단:
  [검색] [🔔 3] [⋯]
           ↑ 안 읽은 알림 배지

클릭 → 알림함 열림 (슬라이드 오버):
  ┌────────────────────────┐
  │ 알림함                 │
  ├────────────────────────┤
  │ 📌 07:30 - 타이레놀    │
  │    복용완료 버튼 누르기│
  │                        │
  │ 📋 05:20 - 주간 리포트│
  │    이번 주 복약 정리   │
  │                        │
  │ 🏥 어제 - 공지사항    │
  │    새 건강 정보 올림  │
  └────────────────────────┘
```

**구현**
- 신규 `notification_logs` 테이블 (마이그레이션 0044)
- `PushService.send_to_profile()` 한 곳에 훅 추가 → 모든 알림이 자동으로 저장
- 웹푸시/FCM 구독이 없어도 인앱 알림함에는 항상 남음

### 2. 알림 클릭 → 관련 화면으로 바로 이동 (딥링크)

**구현**
- `NotificationLog` 테이블에 `link_url` 컬럼 추가 (마이그레이션 0048)
- 각 알림 종류별로 이동 경로 매핑:

| 알림 종류 | 이동 경로 |
|---|---|
| 복약 알림 | `/alarms` (약물 상세) |
| 주간 AI 리포트 | `/weekly-reports` |
| 복약순응도 리포트 | `/track` |
| 습관 목표 달성 | `/habit-selection` |
| 공지사항 | `/notices` |
| 챗봇 답변 도착 | `/chat` |
| 부작용 안내 | `/chat?autoMessage=...` |

**프론트 동작**
```typescript
// NotificationBell.tsx
const handleNotificationClick = (notification) => {
  if (notification.link_url) {
    closePanel();  // 알림함 닫기
    navigate(notification.link_url);  // 해당 페이지로 이동
  }
};
```

**특징**
- `link_url` 없으면 기존 `consult_url` 자동 재사용 (기존 호출부 수정 불필요)
- 알림함 열 때 자동으로 전체 "읽음" 처리

**테스트**
- 공지사항 알림 클릭 → `/notices` 이동 확인
- 부작용 안내 알림 클릭 → `/chat`으로 이동 + 자동 질문 전송 확인

**DB 마이그레이션**
- 0044: notification_logs 테이블 신설
- 0048: link_url 컬럼 추가

---

## 슬라이드 12 — A④ 알림 시스템: 안드로이드 호환성 안정화

**문제**
- 안드로이드 Chrome: 테스트 알림 버튼이 `new Notification()` 직접 호출로 예외 발생
- 알림 켜기 실패해도 "성공" 메시지만 표시 (사용자 혼란)

**해결 (PR #298)**

```typescript
// Before (X)
new Notification("테스트", { ... })  // 안드로이드에서 예외

// After (O)
registration.showNotification("테스트", { ... })  
// 실제 푸시 수신 시 service-worker.js가 쓰는 것과 동일한 방식
```

**개선 사항**
1. 테스트 알림: `registration.showNotification()` 사용
2. 실패 피드백: "알림 켜기"가 실패하면 에러 메시지 명확하게 표시

**테스트**
- npm 빌드/타입체크 통과
- 안드로이드 실기기에서 실제 푸시 수신 확인

---

## 슬라이드 13 — B① 개인화 시스템: 데이터 아키텍처 (건강정보 설계)

**마이그레이션 0057: 민감 정보(키/몸무게) 분리, Profile과 1:1 관계로 설계**

**HealthProfile 모델 (건강 정보 분리)**
```python
class HealthProfile(Base):
    profile_id: BigInteger (PK, FK → profiles.id)
    height_cm: float (신장, cm)
    weight_kg: float (체중, kg)
    gender: Enum (Gender.MALE / FEMALE)
    birth_date: date (나이 계산용)
    diseases: list[Disease] (등록한 질병)
```

**마이그레이션 0058: 칼로리 목표 캐싱**
```python
class DietKcalReason(Base):
    profile_id, log_date (복합 유니크)
    reference_kcal: 개인화된 기준 칼로리
    reason: AI 생성 설명 텍스트
```

**설계 포인트**
- PII(개인식별정보) 분리: 키/몸무게는 HealthProfile에 별도 저장
- 동일 일자 내 재조회 시 AI 호출 방지: (profile_id, log_date) 유니크 제약으로 캐싱
- 그 날 안에 건강정보 변경 시 캐시 갱신: reference_kcal 값으로 변경 감지

---

## 슬라이드 14 — B② 개인화 시스템: 식단 기록 개인화

**개인화된 칼로리 목표 = Mifflin-St Jeor 공식 × 캐싱 × AI 설명**

### 1. 개인화 칼로리 계산 (Mifflin-St Jeor)

```
BMR = 10×W + 6.25×H - 5×AGE + (GENDER_CONST)
      (W=kg, H=cm, AGE=years)

REFERENCE_KCAL = round(BMR × 1.375 / 50) × 50
                (1.375: 일반인 활동계수, 50kcal 단위 반올림)
```

**구현 현황**
- 높이/몸무게/성별/나이 전부 있음 → 개인화 계산
- 하나라도 빠짐 → 폴백 2000kcal (일반 성인 기준)
- 실측 예: 남성(175cm, 70kg, 30세) → 약 2,950kcal

### 2. AI 기반 한 줄 이유 자동 생성

```python
def _get_kcal_reason(profile_id, log_date, reference_kcal, personalized):
    # DB 조회 → 캐시 있으면 반환
    if cached := repo.get_kcal_reason(profile_id, log_date):
        return cached['reason']
    
    # AI Worker 호출 (DietKcalReasonSummary 스키마)
    ai_reason = ai_worker.call(...) 
    
    # 캐싱 (동일 날 재조회 시 스킵)
    repo.save_kcal_reason(profile_id, log_date, reference_kcal, ai_reason)
    return ai_reason
```

**폴백 텍스트** (AI 실패 시)
- 개인화됨: `"키 170cm, 몸무게 65kg을 기준으로 계산된 목표입니다."`
- 폴백됨: `"일반 성인 기준 일일 권장 섭취량입니다."`

**프론트엔드 표시**
- 칼로리 바 아래: `✨ {reason}`
- 건강정보 미입력 시: `📏 키/몸무게를 먼저 입력해주세요` + 링크 버튼

### 3. 게이팅: 건강정보 미입력 시 자동 유도

```typescript
// DietLogContent.tsx
const [missingBodyInfo, setMissingBodyInfo] = useState(false);

useEffect(() => {
  healthInfoApi.get().then(info => {
    setMissingBodyInfo(!info.height_cm || !info.weight_kg);
  });
}, []);

if (missingBodyInfo) {
  return <MissingHealthInfoCard onNavigate={() => navigate('/health-info')} />;
}
// 정상 식단 기록 UI 렌더링
```

---

## 슬라이드 15 — B③ 개인화 시스템: 습관 추천 우선순위

**질병 관련 습관을 먼저 노출, 그 다음 기본 습관 (로테이션 유지)**

### 문제 상황

사용자가 "당뇨, 오른쪽 팔꿈치 골절" 등록 → 기본 습관만 추천됨 (질병 관련 추천 안 됨)

### 해결책: 습관 분류 및 우선순위화

```python
class HabitDef:
    key: str
    label: str
    is_disease_related: bool = False  # 플래그 추가

# 질병별 대응 습관
DISEASE_HABITS = {
    Disease.DIABETES: HabitDef(key="diabetes_check", ..., is_disease_related=True),
    Disease.FRACTURE: HabitDef(key="fracture_rehab", ..., is_disease_related=True),
    # ... 총 5개 질환 × 각 1개 = 5개 질병 습관
}

def pick_recommendations(profile_id, max_count=5):
    pool = get_available_habits(profile_id)
    
    # 1단계: 질병 관련 습관 분리
    disease_habits = [h for h in pool if h.is_disease_related]
    base_habits = [h for h in pool if not h.is_disease_related]
    
    # 2단계: 각 그룹 내에서 로테이션 (결정론적)
    selected = []
    for habit_list in [disease_habits, base_habits]:
        rotation_idx = (today.toordinal() + profile_id) % len(habit_list)
        selected.append(habit_list[rotation_idx])
    
    return selected[:max_count]
```

**주요 포인트**
- 질병 관련 습관을 **우선적으로** 추천 (사용자 건강 상태와 일치)
- 로테이션 유지: 같은 날 같은 사용자는 항상 같은 습관을 추천받음 (예측 가능)
- AI 생성 습관도 `is_disease_related=True` 플래그: 백엔드에서 동적 추천 시 우선순위 적용

**테스트 커버리지**
- 질병 3개 등록 시 최소 1개 이상 질병 습관 추천 확인
- 질병 미등록 시 기본 습관만 나옴 확인
- 매일 다른 습관 로테이션 동작 확인

---

## 슬라이드 16 — B④ 개인화 시스템: 주간 리포트 BMI 포함

**주간 리포트 생성 시 사용자 신체 정보 포함, BMI 자동 계산**

### 주간 리포트 구조

```
🌟 [사용자명] 님의 이번 주 생활 습관 요약

📊 신체 정보:
   키 170.00cm, 몸무게 65.00kg (BMI 22.5)

🎯 이번 주 목표:
   [목표 내용]

📈 지표:
   - 평균 칼로리: 1,850kcal
   - 운동량: 5회
   - 수면: 7시간
```

### 백엔드 구현 (Python)

```python
def _body_info_line(profile) -> str:
    health = profile.health_profile
    if not (health and health.height_cm and health.weight_kg):
        return ""
    
    height = health.height_cm
    weight = health.weight_kg
    bmi = weight / (height / 100) ** 2
    
    return f"키 {height:.2f}cm, 몸무게 {weight:.2f}kg (BMI {bmi:.1f})"

def _build_stats_lines(profile, week_logs) -> list[str]:
    lines = []
    
    # 1. 신체 정보 라인
    body_line = _body_info_line(profile)
    if body_line:
        lines.append(f"📊 신체 정보:\n   {body_line}")
    
    # 2. 통계 라인 (칼로리, 운동, 수면 등)
    lines.append(f"📈 지표: ...")
    
    return lines

def generate_weekly_report(profile_id, week_start):
    profile = get_profile_with_health(profile_id)
    week_logs = get_week_logs(profile_id, week_start)
    
    stats_lines = _build_stats_lines(profile, week_logs)
    report_text = "\n".join(stats_lines)
    
    return WeeklyReportResult(text=report_text, sent_at=now())
```

**테스트 예시**
```python
def test_body_info_line_includes_bmi():
    health = FakeHealthProfile(height_cm=170, weight_kg=65)
    profile = FakeProfile(health_profile=health)
    
    line = _body_info_line(profile)
    
    assert "키 170" in line
    assert "몸무게 65" in line
    assert "BMI 22.5" in line  # (65 / 1.7²)
```

---

## 슬라이드 17 — 시연 영상 (시나리오 제안)

*아래는 구현된 알림 시스템 + 개인화 기능을 5~7분 안에 보여줄 수 있는 시연 시나리오입니다.*

---

### A. 알림 시스템 시연

**1단계: 복약 알림 & 스마트 묶음 발송**
```
(1) 약물 등록: "타이레놀 500mg 09:00 매일"
(2) 약물 등록: "종합비타민 09:00 매일"
(3) 시간 경과 → 09:00 푸시 알림 1개 수신
    "타이레놀, 종합비타민 드실 시간이에요!"
    
액션 버튼:
[ 복용완료 ]  [ 빈도 줄이기 ]
```

**2단계: 스누즈 (미루기)**
```
(1) 알림 본문 탭 → 바텀시트 열림
[ 30분 후에 ]
[ 1시간 후에 ]

(2) "30분 후에" 선택
(3) 30분 후 자동 재알림 수신
```

**3단계: 알림함 & 딥링크**
```
(1) 홈 상단 🔔 버튼 클릭 → 알림함 열림
    - 09:00 타이레놀 알림
    - 어제 주간 리포트
    - 어제 공지사항
    
(2) "타이레놀 알림" 클릭 → /alarms 페이지로 이동 (약물 상세)
(3) "주간 리포트" 클릭 → /weekly-reports로 이동
```

**4단계: 라이프스타일 팁 커스터마이징**
```
(1) 설정 → 알림
(2) "라이프스타일 팁 알림" 토글 ON
(3) 하위 옵션 노출:
    - 시간대: 09:00 ~ 21:00 (조정 가능)
    - 빈도: 3일에 한 번 (선택)
(4) 저장 → 설정 반영됨
```

---

### B. 개인화 시스템 시연

**1단계: 건강정보 미입력 → 식단 기록 진입 불가**
```
(1) 앱 홈 화면
(2) "오늘의 식단" 카드 클릭
(3) "키/몸무게를 먼저 입력해주세요" 메시지 + 버튼
(4) 버튼 클릭 → '/health-info' 페이지로 이동
(5) 키 / 몸무게 / 질병(당뇨, 고혈압) 입력 후 저장
```

**2단계: 건강정보 입력 후 개인화된 칼로리 추천**
```
(1) 설정 → 건강정보 → 키/몸무게/질병 입력
    예: 170cm, 65kg, 남성, 생년월일 1996-07-30
    질병: 당뇨, 고혈압
    
(2) 다시 식단 기록 진입 → 정상 진입됨
(3) 칼로리 바 아래에 개인화 텍스트 표시
   "✨ 키 170cm, 몸무게 65kg을 기준으로 계산된 목표입니다."
   또는: "✨ 당신의 활동 수준을 고려하면 일일 약 2,300kcal 정도가 적당합니다."
   
(4) 실제 음식 입력 → 칼로리 합산 (개인화 목표값 기준)
```

**3단계: 습관 추천 시 질병 우선순위**
```
(1) 홈 화면 → "추천 라이프 스타일" 카드 클릭
(2) 습관 추천 목록 모달 열림
   - 상단: 질병 관련 습관 
     · 당뇨: "매일 혈당 체크"
     · 고혈압: "저염식 식사"
   - 하단: 기본 습관 (명상, 스트레칭 등)
   
(3) 5개까지 선택 → 저장
(4) 마이다이어리 → 선택한 습관별로 매일 기록
```

**4단계: 주간 리포트에 BMI 포함**
```
(1) 마이다이어리 → "주간 리포트" 확인
(2) 리포트 상단에 신체 정보 라인 표시
   "📊 신체 정보: 키 170.00cm, 몸무게 65.00kg (BMI 22.5)"
   
(3) 통계와 함께 종합 분석 메시지 포함
   - 평균 칼로리: 2,100kcal
   - 운동량: 4회
   - 수면: 7시간
```

---

## 기술적 하이라이트

### 알림 시스템

**1. 스마트 묶음 발송 (F-NTFY-2)**
- 같은 시각 알림을 자동으로 묶어 발송 → 사용자 피로도 ↓, 배터리 소비 ↓
- 서비스워커가 각 항목별로 액션 반복 → 단일 탭으로 여러 약 한 번에 처리 가능

**2. 스누즈 재알림 (F-NTFY-3)**
- 1회성 `snooze_reminders` 테이블: 예약 후 자동 삭제로 DB 부하 최소화
- push_scheduler 매분 틱마다 처리 → 네트워크 불필요 (로컬 스케줄링)

**3. 알림함 통합 (PR #299)**
- 모든 알림을 단일 `notification_logs` 테이블에 저장
- PushService 한 곳의 훅 추가 → 알림 종류별 별도 코드 불필요 (확장성 ↑)
- 웹푸시/FCM 없어도 인앱 알림함에는 항상 저장 (안정성 ↑)

**4. 딥링크 라우팅 (PR #302)**
- `link_url` 매핑 → 알림 클릭 후 관련 페이지로 즉시 이동
- 기존 호출부 `consult_url` 자동 재사용 → 레거시 호환성 유지

### 개인화 시스템

**1. 개인화 + 캐싱 (비용 효율)
- AI Worker 호출 비용 절감: 같은 날 1회만 AI 호출, 이후는 DB 캐시 사용
- Race condition 안전: `(profile_id, log_date)` 유니크 제약으로 중복 저장 방지

### 2. 폴백 설계 (안정성)
- 불완전한 데이터(높이 미입력 등)도 서비스 가능: 2000kcal 기본값 + 폴백 텍스트
- AI Worker 실패 시에도 사용 불가능한 상태 없음: 규칙 기반 텍스트 생성

### 3. 라우팅 최적화 (UX)
- 식단 기록 진입 전 건강정보 게이팅: 불완전한 데이터로 인한 오류 방지
- 홈 → 습관 선택 모달 직접 열림: 기존 마이다이어리 허브 전체 로드 제거

### 4. 테스트 커버리지
- 단위 테스트 16개: 모든 엣지 케이스(데이터 미입력, AI 실패 등) 커버
- 마이그레이션 2개: 무중단 배포 가능

---

## 체감한 한계 및 다음 과제

### 알림 시스템
- **액션 버튼 제약**: 브라우저 푸시 API 한계로 액션 2개만 가능 → 스누즈는 인앱 바텀시트로 우회
- **웹푸시 구독 초기화**: 부분 사용자가 웹푸시 미구독 상태 → 인앱 알림함 수동 오픈으로 대응하지만, 구독율 관리 필요
- **빈도 설정 정교성**: 현재 "N일 간격" 간단한 규칙 → 요일/시간대 조합 스케줄링 고려 필요

### 개인화 시스템
- **AI 응답 품질**: AI Worker가 항상 의료적으로 정확한 한 줄을 생성하는 건 아님 → 응답 필터링 / 도메인 파인튜닝 고려
- **캐시 전략**: 일일 캐시는 그 날 안의 건강정보 변경 감지가 정확하지만, 주간 리포트는 주 단위 집계와 불일치 가능 → 집계 시점 명확화 필요
- **습관 데이터 확충**: 현재 질병별 1개씩만 매핑 → 질병당 3-5개 옵션 추가 시 추천 다양성 ↑
- **영양학적 정확성**: Mifflin-St Jeor 공식은 근사치 → 활동계수 개인화(1.2~1.9 범위) 고려 여지 있음

---

## 참고 — 발표에서 "있어보이게" 만드는 포인트

### 알림 시스템 강조점

1. **피로도 감소 수치**
   - "같은 시각 약 2개 → 알림 **1회로 통합** (배터리 소비 ↓, 알림 피로도 ↓)"
   - "브라우저 액션 제약(2개)을 스누즈 바텀시트로 **우회** → UX 역전"

2. **구현의 효율성**
   - "PushService 한 곳의 훅 추가 → **모든 알림 종류 자동 커버** (복약/공지/리포트/챗봇 등 확장성 ↑)"
   - "알림함에 link_url 매핑 → 클릭 즉시 **관련 페이지로 이동** (발견성 ↑)"

3. **안정성**
   - "6개 PR(280/282/298/299/302/323), **0개 회귀** — 테스트 기반 설계"
   - "웹푸시/FCM 없어도 인앱 알림함에는 **항상 저장** (온라인/오프라인 모두 안정)"

### 개인화 시스템 강조점

1. **구체적 수치로 말하기**
   - "마이그레이션 4개 추가(0044~0048 알림함+칼로리, 0057 건강정보 분리, 0058 칼로리 캐시)"
   - "테스트 20개 작성 (알림 로직 포함 시 총 테스트 커버리지 확대)"
   - "AI 캐싱으로 Worker 호출 **1회/일 → 평균 1/5 비용 감소**"

2. **문제 → 해결 서사 강조**
   - "식단 기록할 때마다 건강정보 다시 물어보던 불편함 → **게이팅으로 필수 입력 강제**"
   - "질병 등록해도 기본 습관만 추천되던 문제 → **우선순위 플래그**로 질병 관련 습관 먼저 노출"
   - "알림을 받아도 어느 화면 가야 할지 몰랐음 → **딥링크로 즉시 이동**"

3. **"개인화"의 구체성**
   - 단순 "개인화 칼로리" 아님 → "키/몸무게/성별/나이 기반 **Mifflin-St Jeor 공식** 적용"
   - 단순 AI 생성 아님 → "AI + 폴백 템플릿으로 **100% 가용성** 보장"
   - 단순 습관 추천 아님 → "사용자의 **질병 데이터** 기반 맞춤형 습관 우선순위"
