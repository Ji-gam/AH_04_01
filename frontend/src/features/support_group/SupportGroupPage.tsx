import PlaceholderPage from "../../components/ui/PlaceholderPage";
export default function SupportGroupPage() {
  return <PlaceholderPage title="서포트 그룹 / 리더보드" apiFile="src/api/endpoints/supportGroup.ts" />;
}
